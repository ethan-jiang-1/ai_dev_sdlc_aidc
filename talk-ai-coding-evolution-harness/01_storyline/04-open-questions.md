# 已决 / 待办（随手记）

> 每次进来说完话，把状态落回这里。**已决**要写清"什么时候、为什么这么定"。

## 已决

| # | 事项 | 决定 | 时间 |
|---|---|---|---|
| D1 | 对象 | 团队协作开发、交付物对可靠性/合规/审计要求高、质量门槛高的软件团队；**稿子里不点行业名、不用行业专属术语**（换观众不用改稿） | 2026-09-16 |
| D2 | 引用禁令 | **全场 audience-facing 文字不出现 OPC / 一人公司 / 姊妹项目** | 2026-09-16 |
| D3 | 篇幅 | 75–90 min · **正文 45 页 + 4 张停顿页（D27）= 49 张 slide**，按约 85.5 min 排 | 2026-09-16 |
| D4 | 视觉方向 | 纸白底 + **代码终端母题**：等宽字体承担代码/命令/日志/指标；深墨结构线；青绿·朱红·琥珀只做语义强调 | 2026-09-16 |
| D5 | 图源策略 | **全原生矢量**，不依赖 AI 生图（清晰、零错字、投影稳、技术场更专业） | 2026-09-16 |
| D6 | 叙事脊柱 | 生成不是瓶颈 → 交付才是 → 取决于模型外那圈 → 团队里那圈最容易散 → 那圈叫 harness，它是变量 | 2026-09-16 |
| D7 | 尾巴位置 | **第三幕 → 收尾 = 22 页 / 约 42 min**，占全场 52%（前半场 23 页是引桥） | 2026-09-16 |
| D8 | 团队版论证 | 替代个人视角的"最终控制权"，改用 **一致性**：十个人十套配置 = 十套交付系统，harness 把"靠人品"变成"靠门禁" | 2026-09-16 |
| D9 | 证据纪律 | 外部数字逐页带证据强度标注（一手 / ⚠️ 厂商自述 / ⚠️ 降级取证 / 趋势级）；星数与插件数标观测日期，只作趋势 | 2026-09-16 |
| D10 | 反软广纪律 | 第四幕 Pi 与 DSH 代价同屏；第五幕必须包含"什么时候别折腾" | 2026-09-16 |
| D11 | 素材扩充 | 增挂 `rawdata_pi-digested`（Pi 源码消化）与 `rawdata_pi-faq-on-digested`（含 Pi vs DSH 逐面对照）→ 第四幕 Pi 页从**二手报道**升级为**带源码锚点的一手材料** | 2026-09-16 |
| D12 | Pi 的 MCP 口径 | 两份素材冲突（选型研究引 PR #3774 vs 消化层源码级核对"无 MCP/ACP 实现"）→ **采信有源码锚点的一方**：讲"生态锁在自家 extension API"，不把该 PR 当已 GA | 2026-09-16 |
| D13 | Pi 的"无沙箱"口径 | 不说"Pi 不安全"，说 **"Pi 把信任放在『谁装了它』，而不是『它是什么代码』上"**；并区分"沙箱执行工具"（有思路）与"沙箱运行扩展代码"（无机制） | 2026-09-16 |
| D14 | **Pi / DSH 平权** | 用户明确：两边素材都得有。核对后两边料等厚（Pi 154 篇 / DSH 173 篇，且选型研究中两侧各有一个并列深挖专题）→ **取消不对称方案，改为平权**：DSH 三页、Pi 三页，中间加 **P37「两边都还没解决的那件事」** | 2026-09-16 |
| D15 | 第四幕重排 | P31 分野 → P32 DSH 无特权核心 → P33 DSH 会话日志 → P34 DSH 代价 → P35 Pi 结构 → P36 Pi 代价 → P37 两边都没解决 → P38 共同点。原"四种运行模式""长程不是单一 loop"两页并入 P32 | 2026-09-16 |
| D16 | 目录职责简化 | 取消 `03_outline/` 下的五个逐幕内容文件（与 `04_drafts/ppt-text-vN.md` 内容重叠）。**页面职责**留在 `03_outline/`，**页面文字与讲点**只写一处（`04_drafts/`） | 2026-09-16 |
| D17 | 逐页文案 v1 | 45 页生产事实稿已铺满（`04_drafts/ppt-text-v1.md`），含证据强度标注与全篇自检清单 | 2026-09-16 |
| D18 | **页面承载规划** | 用户反馈 v1"内容太少、想不出怎么展现"→ 新增 `03_outline/01-visual-carrier-plan.md`：定义每页四层承载（L1 结论标题 / L2 主视觉载体 ≥40% / L3 支撑 / L4 脚注）+ 七类载体 + 45 页逐页载体分配。**抽掉标题后页面若不空，才算合格** | 2026-09-16 |
| D19 | **v1 的两处修订方向** | ① 讲点从 1–2 句扩到 **150–250 字/页**（含具体例子或听众场景），能撑 2 分钟；② 上屏文字按 L1/L3 重排，标题即结论、要点只放与主视觉咬合的。→ 落在 `ppt-text-v2.md` | 2026-09-16 |
| D20 | 视觉样片 | 已产出 4 张交互稿（封面 / 机制页 P16 / 数据页 P25 / 对照表页 P31），**用户确认"信息浓度可以"** | 2026-09-16 |
| D21 | **页面文案 v2** | 按四层承载重写 45 页（`04_drafts/ppt-text-v2.md`）：每页 L1 标题 / L2 载体 / L3 支撑 2–5 条 / L4 页脚。v1 保留作对照 | 2026-09-16 |
| D22 | **讲稿与页面分文件** | 页面规格留 `ppt-text-vN.md`（求"抽掉标题后不空"）；口语讲稿独立成 `talk-vN.md`（求"能连着说两分钟不卡"）。**讲稿目标 250–400 字/页**（中文演讲约 220 字/分钟，扣除停顿与互动），全场约 12,000–14,000 字 | 2026-09-16 |
| D23 | **讲稿字数改为按时长加权**（覆盖 D22 的一刀切） | 84 min ÷ 45 页 = 平均 1.87 min/页，一刀切会让 3 min 的 P7 与 2.5 min 的 P16/P17/P25/P32/P33/P35 偏薄、0.5 min 的转场页偏厚。**改为 `每页字数 ≈ 时长 × 220 × 0.8`**：全场约 **14,700 字**，尾巴约 **7,400 字**；短页约 90 字、长页约 530 字。0.8 是给看图、停顿与互动留的余量 | 2026-09-16 |
| D24 | **讲稿起步方式** | 不直接铺满尾巴，**先出 P24 + P25 两页样张**定语气与密度，确认后再铺 22 页。样张已落 `04_drafts/talk-v1.md` | 2026-09-16 |
| D25 | **讲稿字数分段预算** | 页面时长账目不动（合计 84 min 正确），**只收紧讲稿字数**：**引桥 P1–P23 按 39 min / 约 6,860 字**（不是账面的 42），**尾巴 P24–P45 按 42 min / 约 7,400 字**。余出约 3 分钟作现场缓冲——开场最容易超时、第二幕机制页最容易讲开 | 2026-09-16 |
| D26 | **PPT 生产事实稿新增 `讲法`（不上屏）字段** | L1–L4 是**上屏内容**；讲者的自我提醒、顺序纪律、停顿安排改用 `**讲法**（不上屏）` 字段承载，由 `talk-v1.md` 吸收。修掉 8 页 L3 里混着讲法提示的问题 | 2026-09-16 |
| D27 | **新增 4 张停顿页 I1–I4（不参与正文编号）** | 用户提出用"停顿页"解决密集块无呼吸的问题（第一幕 17 min / 第二幕 20 min 中间一次呼吸都没有）→ 在 **P8 / P17 / P20 / P34 之后**各插一张单句大字页：留白 ≥60%、**不显示页码**、20–30 秒、念完即静默。**正文 45 页编号完全不动**，所有既有页引用保持有效；总 slide 49 张、总时长 84 → 约 85.5 min（仍在 75–90）。硬约束：**每个信息块结束后要有一次呼吸、单块不超过 10 分钟** | 2026-09-16 |
| D28 | **补挂「一手来源卡片层」+ 三处回源核对全部完成** | 发现 `_reference/` 此前只接了综合层（final 报告）与消化层（pi/dsh 源码消化），**漏了中间的 305 张一手来源卡片**——这正是 P6/P7 长期"待回源"的真正原因（要核的料不在库里）。新增两条只读 symlink：`rawdata_ai-coding-evolution-reference`（137 张）、`rawdata_harness-selection-reference`（168 张）；素材 463 → **768 文件**。**P6 / P7 / P25 回源全部定稿**（含可直接引用的英文原句），并顺带挖到可加固 P5 / P12 / P15 / P24 / P26 的素材 | 2026-09-16 |
| D29 | **4 张视觉样片已落成文件** | 此前那 4 张只在旧对话里、无文件。用户要求重建 → 产出 `05_output/v0.1/visual-samples.html`（浏览器打开即看）：**封面（P1）+ 机制页（P16）+ 数据页（P25）+ 停顿页（I3）**。第 4 张**刻意用停顿页替换原对照表**——对照表是最标准版式、风险最低；停顿页是本次新机制、最容易做歪，必须先进样片。页首附「视觉规格速查」，末附"请重点看这四件事"。**这 4 张成为后续 45 页的视觉基准** | 2026-09-16 |
| D30 | **全套 49 张已产出（v0.2）** | 样片获认可后，用户指示"重新全都生产"。产出 `05_output/v0.2/deck.html`：**45 张正文 + 4 张停顿页**，与 v0.1 同一套视觉语言；含幕分组索引、逐页「页名·时长」标注、**逐页可见的 `讲法`（不上屏）**；停顿页不显示页码。**采用生成器方式**（`.tmp-harness-talk-deck/build_deck.py`，临时目录不入库）——改稿改生成器再重跑，避免手改 49 处导致版式漂移。**完整 PPTX 待内容与视觉定稿后转** | 2026-09-16 |

## 待用户确认（**已按建议值先行，可随时否决**）

> 用户 2026-09-16 指示"继续往下做"。以下四条**按建议值临时生效**，已写进 `04_drafts/ppt-text-v1.md`；
> 一旦否决，只需改这一处 + 反向下游同步，成本很低。

| # | 事项 | 当前临时值 | 状态 |
|---|---|---|---|
| Q1 | **开场两句钩子** | 采用："AI 能写代码早就不是问题了，能不能上线才是问题" + "团队里最会写 prompt 的那个和新人，交出来的质量为什么不一样？" | 临时生效 |
| Q2 | **第一幕时长** | **17 min**（不砍到 15，因有产品经理在场） | 临时生效 |
| Q4 | **题目** | 「AI Coding 演变指南：从 Prompt 到 Harness」+ 副标题「生成不是瓶颈，交付才是」 | 临时生效 |
| Q5 | **收尾 Slogan** | **「能力看模型，差距看 harness」** | 临时生效 |

## 待办（不阻塞）

- [ ] **讲稿 `04_drafts/talk-v1.md`（当前唯一任务）**：45 页口语讲稿。
      **字数 ≈ 时长 × 220 × 0.8**（全场约 14,700 字，其中尾巴约 7,400 字；短页 90 字、长页 530 字）。
      执行顺序：**P24–P45 → P14–P23 → P1–P13**。规格见 `../HANDOFF.md` 第六节。
- [ ] 第三幕的"五个自查维度"需要在 `02_evidence/` 里补：现在主流的固定 harness 各自开放了什么合同、
      没开放什么——素材里只有 Codex 的接口形态（exec / SDK / app-server）与 Claude 系的权限策略，
      其余产品的边界需要补证或改成不带产品名的通用说法。
- [ ] **P32 密度偏高**（原"第四幕机制页"待办，经 D15 合并后集中到这一页）：一页要同时承载
      无特权核心 + 四种运行模式 + 三种长程接力，对 PM 偏重。讲稿里只挑一条讲透，其余一句带过。
- [ ] 第五幕"什么时候别折腾"需要一段可执行的判断，现在只有方向（边界是否开始限制真实交付）。
- [x] **第一幕 Context 那页的"最小脚手架"证据已回源核对（2026-09-16）**：一手原文
      `keep the scaffolding minimal. The agent has a prompt, a Bash Tool, and an Edit Tool.`
      （Anthropic SWE-bench Verified 公告 2024-10-30，49%）。**P6 / P7 / P25 三处回源全部完成**，
      定稿引文与出处见 `02_evidence/00-absorption-plan.md` 第五节 5.2。
- [ ] **新挖到、可加固别的页、尚未上屏的素材**（登记在吸收清单 5.3，等用户拍板要不要用）：
      P12 的团队版论点有外部素材了、P5 可换成"每层对下层做一个假设"、P15 有 Böckeler 一手原句、
      P24 有"同一模型换脚手架"的更早先例。
- [x] 视觉样片：4 张（封面 / P16 / P25 / P31）已产出并被用户确认"信息浓度可以"（D20）；
      但**只在旧对话里、尚未落成文件**，需要时按视觉规格重建。
- [x] 全篇 45 页页面文字：`ppt-text-v2.md` 已铺满（D21）。

## 内容 REVIEW 发现（2026-09-16 · 全文过一遍的结果）

### 已当场修掉的（机械性，改回成本低）

- **术语统一**：`CONTEXT.md` 定义的是**运行边界**，但全库 8 处在用**执行边界**（storyline-map、
  thesis A3/立场、audience-and-pitch、carrier-plan、ppt-text-v2、HANDOFF、CONTEXT 禁用词行）→ 已全改；
  并在 `CONTEXT.md` 的"不要混用成"一栏补上"也不要写成执行边界"。
- **内部词漏上屏**：`ppt-text-v2` P4 的 L1 原为"前半场是**引桥**，后半场是正题"——"引桥"与"尾巴"
  同属内部叙事用词 → 已改为"前半场把问题逼出来，后半场给答案"，并在 `CONTEXT.md` 第三节补「引桥」条目。
- **自造证据档位**：P25 的"厂商一手自述"、P27 的"多为厂商文档主张"、P31 的"媒体共识口径"
  都不在四档之内 → 已统一为 `⚠️ 厂商自述` / `⚠️ 趋势级`。
- **第四幕七页缺强度标注**：P31–P37 全是外部机制主张却没有四档标注（规则本要求"与数字同屏"）→ 已补：
  P32/P33 一手（源码级）、P34 一手（官方状态）+ ⚠️ 趋势级（媒体口径）、P35/P36 一手（源码锚点，
  并标明是**评价文档**、硬事实与解释分开标，扣红线 15）、P37 标为"二次消化素材 + 本 talk 判断"。
- **过期状态**：`00-storyline-map.md` 头部仍写"页面文字未开始"；`02_evidence/README.md` 写
  "十二条红线"（实为十五条）且引用了不存在的 `01-info-flow-map.md`；`04_drafts/README.md`
  未列 `talk-v1.md` 且仍写 250–400 字 → 均已改。

### R1–R8 的处理结果（2026-09-16 第二轮 · 已自行裁决并落盘）

| # | 发现 | 处理 |
|---|---|---|
| R1 | P26 花 2 分钟只承载一个"自己都说不能引用"的来源，信息量近乎零 | **保留页数与"证据二"身份，重新定位为「现象层」**：它证的是**现象**（跨 harness 会分层）不是**数字**，本 talk 不引用它任何具体数字；主视觉右侧加"**能当基准 / 只能当信号**"两栏判据，把这一页变成听众可迁移的判断工具。删页会破坏 D3 的 45 页规格并引发全篇重编号，不做 |
| R2 | 五问是自拟框架却占 4 分钟 | **已在三处认领**：P28 / P29 的 L4 写"本 talk 框架（从评分卡拆出，**非行业标准**）"；`03_outline` 必讲要点加"必须认领"；`talk-v1.md` 纪律第 7 条 |
| R3 | P2 的 L1 只有半句 | **撤回**。复核后确认：L1 是断言前半句，后半句由 L2 的两行大字（能写 / 能不能上线）承载，**L1+L2 合读才是完整判断**；把整句压进标题会变长并与主视觉重复。已加 `讲法` 字段把这条判断**锁住**，防止后人当成缺陷去"修" |
| R4 | P6「这层被吃掉了」有冒犯风险 | **已解决**：P6 的 L3 补一条兜底句（"那套东西没有白学——它变成了默认值"），并加 `讲法` 字段；写进 `talk-v1.md` 纪律第 11 条 |
| R5 | P12 与 P24 用同一个观察 | **已解决**：P24 的 `讲法` 字段明确"只用一句回指 P12，不重讲那个画面"；`03_outline` P12 必讲要点写明本页**不用"两个团队"的画面**；纪律第 5 条 |
| R6 | 引桥时间零缓冲 | **已解决**：页面时长账目不动（合计 84 正确），**只收紧讲稿字数**——引桥按 **39 min / 约 6,860 字**、尾巴按 42 min / 约 7,400 字，余出约 3 分钟作现场缓冲。写进 `03_outline` 时间约束 + `talk-v1.md` 头部 |
| R7 | 说"可以换"却不告诉换哪条 | **已解决**：P41 的 L3 加"**本场不替你们选路线——选型的输入是你们自己的边界**"；纪律第 10 条 |
| R8 | 第四幕连续多页无结论升级 | **已解决**：P34 的 L3 落点改为"这些代价不是缺陷，是**你拥有整个运行时的价格**"，承担第四幕第一次升级；完整节拍写进 `03_outline` 叙事检查第 6 条（P33→P34→P36→P37，间隔不超过 4 页） |

### 第二轮复查新查出并修掉的（系统性）

| # | 发现 | 处理 |
|---|---|---|
| S1 | **L3 层混进了"讲法提示"**（自我提醒、顺序纪律、停顿安排），共 8 页：P2 / P8 / P24 / P27 / P29 / P30 / P34 / P39。L3 是**上屏内容**，混着写会让 PPT 生产时不知道哪些该上屏 | 文件头补"两个字段不要混"的说明；这 8 页新增 `**讲法**（不上屏）` 字段，把非上屏内容移出 L3。同时把 L3 里原本偏弱的条目换成真正的支撑信息（如 P24 补"差的那圈是谁放行、碰得到什么、留下什么记录"） |
| S2 | 术语变体 **"标准边界"**（P41 的 L3 与页面职责各 1 处） | 已统一为**运行边界**；自检清单补一条"不写执行边界 / 标准边界" |
| S3 | **第三幕是孤证**：P24 的核心判断只由 P25 一条（且是厂商自述）支撑，P26 又"不能引用"——逻辑上撑不住 | **补成三条证据链**：P25（一家厂商自述的历史对比）+ P26（现象：跨 harness 会分层）+ **P27（品类共识：所有成品都把沙箱/审批/事件流/记忆当卖点 = 整个品类在用脚投票）**。P27 的 L3 补落点句并标注"本 talk 推断"；叙事检查第 4 条写死这条链 |
| S4 | **P25 用能力评测去证明"交付系统"**，层次不对 | P25 的 L3 补一条口径："它证的是**能力层**——同一个模型能做成的事变了；交付层由 P27–P29 的边界与第五幕的判据承接" |
| S5 | 叙事检查第 5、6 条重复（都写了"P38 把两条路线收成一句共同点"） | 已合并重排为 8 条，并补上"下限 / 上限"分野（第 3 条）与第四幕升级节拍（第 6 条） |

### 还剩两件真正需要商量的

1. **P26 的存废最终决定权在你**。我按"保住 45 页规格 + 给它一个可迁移的判据"处理了；
   如果你觉得第三幕宁可 6 页更紧凑、把 P26 的 2 分钟还给 P25，那要改 45 页编号——**这一条我没权替你定**。
2. **4 张视觉样片至今只在旧对话里、没落成文件**（封面 / P16 / P25 / P31）。
   进 PPT 生产前必须重建，否则那 4 页的视觉方向没有依据。要不要现在补，等你发话。

## 已排除

- **不做 demo**：正文 45 页 + 4 张停顿页已排到约 85.5 min，现场演示放不下。
- **不引用上游报告的"260 万浏览""50 样本"一类二手量化**：与本场论点无关，且口径存疑。
- **不讲 CVE 编号表**：只讲"授权必须在执行端"的因果链。
