# CURRENT-history.md — 历轮改动冷区（append-only）

> **定位**：本文件是 `CURRENT.md` 的**冷区**，只存已收口的历轮改动记录与当时口径，**不是第二权威**。
> 判断"现在做到哪 / 下一步动哪"只认 `CURRENT.md`；本文件只用于回查某轮改了什么、为什么这么改。
> 冲突时以 `CURRENT.md` 与管道内当前版本为准。条目按时间倒序追加，不删不改。

---

## 入口件一致性修复（2026-09-21 第二轮 fresh-agent review，待用户 review 后提交）

上轮只修了 `CURRENT.md` 与根 `README.md`，本轮把 talk 自己的入口件补齐（未提交，等用户 review）：

1. **`AGENTS.md` 生产工具链节**：整节标注"⚠️ 2026-09-17 起旧路线作废"，指向 `CURRENT.md` 第 0 条
   （handoff 路线）为唯一权威；slidep 备忘收进 `<details>` 折叠（含 `.workbuddy` 路径已废弃的提示）。
   陷阱 #14（upsert-dsl）同标作废。当前状态指针区补 handoff 交付稿条目。
2. **`README.md`**：① 「十三份」→「十四份」（与目录树注释及 `_reference/` 实际 symlink 数一致）；
   ② 工作区地图补 `CURRENT-history.md` 与 `05_output/handoff/`，v0.4 标注降为"内容与页序对照"；
   ③ 两处 v2 页码引用（P25 / P26 flash 卡）改为"页号以 `03_outline/00-page-structure-v3.md` 为准"。
3. **`CURRENT.md`**：删除验收清单残留项「42/42 `slidep validate` 通过」（路线已作废）；
   缺口清单中 P25/P26 v2 页码一条改记"已清理"（根 README 经查无残留）。
4. **`04_drafts/README.md`**：口语讲稿待办的启动条件从"页面文字定稿后开工"改为"review 通过后开工"
   （v3.3 页面文字已出、待 review）。

---

## 指针清理（2026-09-21，fresh-agent 一致性核查）

零上下文 agent 实地核查发现两处失效指针，已修正：

1. **`.tmp-harness-talk-deck-v3/` 已不存在**（生成器与 review 渲染图 shots4/shots5 均已清理）。
   `CURRENT.md` 下一步第 1 条改为"直接开 PPTX 或按 handoff 稿逐页过"，生成器过渡改法标记作废；
   已知缺口区该条改为"已清理，重产 PPTX 走 handoff 路线"。
2. 根 `README.md` 结构树两处漂移同步到 v3.3 口径：harness 改为"42 张 = 37 页正文 + 5 停顿页"；
   opc 由"已归档"改为"已定稿 v8（只作内部参考）"，与第三节状态表一致。

---

## v3.3（2026-09-17 下午，收口于 23:55 状态同步）

用户反馈：**3→4→5 不顺**——P3 甩出三个问号后直接跳到五层总览，中间的**因果**没人说。

1. **新增 P4 桥页**「这三道关不是没人想过 —— 每答一轮，答案就往外挪一层」。
   只做一件事：讲清那三道关与五层的关系（① ② 在模型里面，③ ④ ⑤ 在模型外面），
   **五层只出编号不出细节**。原 P4–P36 **顺延为 P5–P37**（正文 36 → 37 页）。
2. **P5（五层总览）**标出**模型里面 / 外面**的分界，底部改成
   「**五层都还在，但杠杆不在同一层**」——原来那句"是叠加不是替代"会和下一页的"边际收益低"打架。
   并上屏一次本幕的问法（答案放在哪 / 默认了什么 / 什么时候不成立）。
3. **删停顿页 B2**（原插在 Harness 与 Loop 之间）——它是上一页刚说完的话的复述，
   且**把五层从中间劈开**。判据升级为：**连续推导中途不插停顿页**，哪怕这一段已超 10 分钟。
   B3–B6 **顺延为 B2–B5**，停顿页 **6 张 → 5 张**。

**口径（v3.3 定案）**：正文 **37 页（P1–P37）+ 停顿页 B1–B5 = 42 张**；正文 **78 min** + 停顿约 1.7 min
≈ **80 min**（留约 5 min 现场缓冲）。弧线：开场 P1–P3 / 6 · 第一幕 P4–P12 / 19.5 ·
第二幕 P13–P19 / 16 · 第三幕 P20–P25 / 13 · 第四幕 P26–P34 / 17.5 · 收尾 P35–P37 / 6。
**收束页 P12 / P25 / P34 自带停顿**；停顿页落在 **P3 / P16 / P19 / P22 / P28 之后**。
尾巴 = **P20–P37**（18 页 / 36.5 min）；引桥 = **P1–P19**（19 页 / 41.5 min）。

> **★ 这次顺延此前只同步了 6 个文件**（`00-storyline-map.md`、`CONTEXT.md`、
> `04_drafts/README.md`、`05_output/README.md`、`DESIGN.md`、`AGENTS.md` 的一部分），
> 其余状态文件仍停在 v3.2 的 36 页口径。**2026-09-17 23:55 已补齐**：
> `CURRENT.md`、根 `README.md`、`01_storyline/02-turning-points.md`（46 行页号 +1）、
> `01_storyline/03-audience-and-pitch.md`、`03_outline/README.md`、
> `03_outline/01-visual-carrier-plan.md`、`02_evidence/00-absorption-plan.md`，
> 外加 `CONTEXT.md` 的「尾巴 / 引桥」两行（脚本漏了）。
> 依据是生成器侧 `fix_v33_*.py` 的既有约定，**不是重新推导口径**。

## v3.2（2026-09-17 上午）

> 下面页码是 **v3.2 编号**；v3.3 顺延后为 +1。

1. **停顿页**（原"呼吸页"）：名称废弃——讲者行话，听众不理解。六张全部重写，
   每张标型：**反思**（问号收尾）/ **顿悟**（顿号并列）/ **迷惑**（?! / !?），
   且全部改为**大字 + 小字**双层（大字给冲击，小字给前提）。
2. **借力开源 harness 的好处**：新软链 `_reference/rawdata_dsh-plugin-ecosystem/`
   （awesome-dsh-plugin 生态全景，2026-09-15 快照）。P28 加桥接句（在自己手上 ≠ 从零写）、
   P29 重写为「站在开源 harness 上自己拼」（借力好处 + 边界）、P30 加生态证据
   （3722 条 / 33 天 / 增强 51% / 运行与治理 19.7%）、P32 加生态代价（82% 0.x / 7.1% 再动率）、
   P33 加凭据面（549 条 14.8% 提到 api key / 登录 / 授权）。
   口径红线见 `02_evidence/00-absorption-plan.md` 新增小节；⚠️ 趋势级，同页数字同一快照。

## 冲突清理（2026-09-17）

用户反馈："一会儿删一会儿搞"——根因是**同一类产物在目录里留了多个版本**，
旧版页码 / 编号会把下一轮带偏。已按"**版本单一**"原则清理：

**移出本目录**（归档在仓库根 `.tmp-harness-talk-archive-20260917/`，不入库，可回查）：

| 归档件 | 属于 | 为什么移走 |
|---|---|---|
| `03_outline/00-page-structure-45.md` | v2 的 45 页页表 | 与 v3 的 36 页页表直接冲突 |
| `04_drafts/ppt-text-v1.md`、`ppt-text-v2.md` | v1 / v2 页面稿 | 与 `ppt-text-v3.md` 三份并存 |
| `04_drafts/talk-v1.md` | v2 的 P24 / P25 讲稿样张 | 页码失效；**三件套格式已提炼进 `04_drafts/README.md`** |
| `05_output/v0.2/deck.html` | v2 结构 49 张 HTML | 与 v0.4 两套页码并存 |
| `05_output/v0.3/`（含 49 页 PPTX、49 个 .slide） | v2 结构完整 PPTX | 已作废，却仍在目录里当"当前入口"的候选 |
| `05_output/v0.4/.../STORY.md` | **从 v0.3 复制来的 v2 叙事** | 藏在 v0.4 里的旧叙事，最隐蔽的冲突源 |

**收敛双权威**：`HANDOFF.md` **已归档**（2026-09-17）。它仍有效的内容——已知陷阱、生产工具链、
素材速查——已并入 `AGENTS.md`；当前态只认 `CURRENT.md`。

**口径统一**：提议权 → **提出权**；"尾巴 / 引桥"按当时的 36 页重编（尾巴 P19–P36、引桥 P1–P18；
**v3.3 顺延后为尾巴 P20–P37、引桥 P1–P19**）；`03-audience-and-pitch.md` 的时长与结构改为 v3；
三个目录 README 全部重写指向当前版本。

## 渐进式披露改造（2026-09-21）

`CURRENT.md` 长期承载历轮流水，热文件过肥（14KB）。拆分：**热区留 `CURRENT.md`**
（一句话 / 状态表 / 定调 / 下一步 / 验收清单 / 缺口），**冷区移入本文件**（v3.3 / v3.2 / 冲突清理记录）。
约定见根 `AGENTS.md` 第 1、3 节。
