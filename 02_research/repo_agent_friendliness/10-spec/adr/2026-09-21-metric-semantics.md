# ADR 2026-09-21 · 度量语义定标（映射 / 评级刻度 / 门禁 ⚠️ / agent 时代可复现性）

> 状态：已裁决（v1.4 起生效）。落实 framework §4 与设计约束 3 中挂账的四个待定义项。
> 关联：[`../framework.md`](../framework.md) §2.3、§3.1–3.2、§3.5、§4.1–4.4。

## 背景

框架 v1 定型了聚合结构（门禁+加权、短板制），但留了四个未定义项，实际跑审计前必须闭合：
① ✅/⚠️/❌ 无数值映射，短板制公式 `min(均分, 最低×2)` 无法计算，且若 ❌=0 则单条 ❌ 清零整维；
② "D 级"被引用但 A–D 刻度从没成文；
③ ⑤ 门禁层的 ⚠️ 处置未定；
④ 设计约束写"两个审计者结果一致"，而系统已升级为单 agent 执行仪式，承诺对象错位。

## 裁决

### D1 数值映射：✅=1.0，⚠️=0.5，❌=0.25（❌ 不记 0）

理由：加权层的职责是**梯度排序**（哪个仓库/哪个维更差、差多少，喂效标回归），绝对红线归⑤门禁管。❌ 记 0 有两个害处：单条 ❌ 清零整维，维度分退化成准二值，校准失去信噪；全 ❌ 维与"该维不存在"不可区分。0.25 让单 ❌ 把所在维腰斩到 0.5（min(0.917, 0.5)），保留排序信息又不稀释短板信号。映射为经验设定，随 §4.4 校准可修订。

### D2 评级刻度：A–D，按"门禁 + 最弱维"判定（不依赖权重，校准前可用）

| 级 | 判定 | 语义 |
|---|---|---|
| A 示范 | 门禁 ✅ 且所有加权维 ≥ 0.9 | ≈ 全 ✅，偶发 ⚠️ |
| B 健全 | 门禁 ✅ 且所有加权维 ≥ 0.7 | 无 ❌，⚠️ 受控 |
| C 欠账 | 门禁 ✅ 但存在 < 0.7 的加权维 | 有 ❌ 或 ⚠️ 密集维 |
| D 不合格 | 门禁 ❌（⑤ 任一 ❌） | 红线失守 |

取**最弱维**（min）做刻度依据，与 §4.3 短板制的哲学一致：agent 友好度是约束满足问题，不取均值。刻度不需要权重数值——解除了"权重未校准 → 报告无总分"对交付的阻塞（§4.4 相应放开：A–D 照给，加权数值分与总分列暂缺）。

### D3 门禁 ⚠️：不降 D，但总评封顶 B，且必须进整改清单次段

理由：安全有未闭合警告的仓库不应获评"示范"（A 要求门禁 ✅ 级即零 ⚠️）；但 ⚠️ 是"有条件的护栏"而非红线失守，降 D 会与 ❌ 失去区分度。封顶 B 是介于两者之间、且实现为零成本的规则（评级时多查一项）。

### D4 可复现性承诺改写为 agent 执行语境（按 tier 分档）

| tier | 承诺 | 证据要求 |
|---|---|---|
| 0 | 任意执行者复跑一致（脚本化，模型无关） | 命令/脚本原始输出引用 |
| 1 | 同 harness 同版本复跑一致 | 自检命令原始输出引用（如 `/context` 截图/文本） |
| 2 | 同模型同协议方向性一致；跨模型承认评审差异 | 演练记录 + 抽样清单 |

原"两个审计者"是人工审计时代的措辞；agent 执行后，"第二审计者"角色由**同 manifest 复跑**承担——pilot 阶段对 tier-2 条目做双跑验证协议，一致性数据进效标。spirit 不变：诚实分级地承诺可复现，而不是假装全绿。

### D5 不适用（N/A）、条件判据与观察项的度量语义（v2 补）

**背景**：定义层早已预设"不惩罚/不适用"——⑧ A 子族的条件判据（未采用 SDD 不扣分）、⑥②⑨ 的规模分档、B 线探索级"进观察项"——但度量层没有承接语义：schema 无适用性字段，三档无 N/A，短板制与 A–D 的 min 会把"不适用"误当短板。此项不闭合，checklist 落条第一个撞上。

裁决：

1. **schema 增 `applicable` 字段**：`无条件 | 条件（何时适用，一句话）`。
2. **条件不满足 → 记 N/A**：不进均分分母、不进短板 min、无证据义务；报告须列 N/A 条目与理由，无理由的 N/A 视同无证据打分。
3. **整维全 N/A → 该维不参与 A–D 刻度与加权**，报告标注"不适用"及理由（刻度含 N/A 维时，min 会被空维误伤）。
4. **观察项**（B 线探索级判据）：单列呈现，不进三档、不进任何聚合；升"共识"级后入算。

### D6 B 线双路径的评级呈现（v2 补）

**背景**：line-b §5 规定开发时/运行时分数"不合并"，但 §4.2 刻度与 report 模板都是单一评级字段，B 线报告的呈现形态未定义。

裁决：B 线报告产出**两个评级域**——**开发时路径**（九维、同 A 线 A–D 刻度）与**运行时路径**（B 子族判据、同一三档与短板制；探索级判据按 D5 入观察项不计入）。两域分别报告、不合并、不与 A 线横向比较；report 模板以分节承载（bundle-format §4）。

## 后果

- framework §2.3、§3.2（规则）、§4.1–4.4 同步改写；v1.4 变更记录留痕。
- 03/09 维的 gitignore 共享表面、05 维的门禁 ⚠️ 开放题就地闭合（归属与规则写回各维文件）。
- CURRENT 开放题清单相应收缩：余下 ⑨ 一手依据回填、六层映射×⑨ 两项（均为试测后事项）。
- v2 追加：D5 N/A 语义回写 framework §3.1（schema）、§4.1–4.3；D6 回写 §3.5、line-b §5、bundle-format report 模板；dimensions/08 的"无 spec 打分"开放题就地闭合（条件判据记 N/A）。

## 变更记录

- v2（2026-09-21）：追加 D5（N/A / 条件判据 / 观察项度量语义）、D6（B 线双路径评级域）——一致性加固中发现度量层未承接定义层的条件判据语义，framework v1.7。
- v1（2026-09-21）：初版，四项裁决。
