# 当前状态（热区 · 唯一权威）

> 最近一次更新：**2026-09-22（v4）**——一轮「回源 + 去挤」修复（30 处，含 5 处硬伤），
> 并按**20 页预算**重排页表，同时把管道铺到 `03_outline` / `04_drafts`。
> 修复清单见下方「v4 修复清单」；已收口的 v3 轮次移入 `CURRENT-history.md`（冷区）。
> **本轮另经独立复核**：30 处全部落地、证据层独立回源通过；**1 处阻断（我自己的压缩时长算错）+ 6 处残留**已当场修掉，见「v4 独立复核」。
> **v4.1（同日）**：上屏文案过了一遍「说人话」——去翻译腔、英语只留术语，见「v4.1 文案说人话」。
> **v20（同日）**：**第二轮独立复核**（先冻结 7 个 hash 再派）——**上一轮 4 条阻断经复核真修好**，
> 新查出 **3 条阻断 + 7 条重要 + 10 条小**，**已全部落地**，见「v20 第二轮复核」。
> 本文件管**当前态**；`README.md` 管地图；`CONTEXT.md` 管术语与红线。

## 一句话

**v4 已全部落盘（故事线 + 证据 + 术语 + 03 页结构 + 04 PPT 事实稿），等你 review。
PPTX 尚未渲染（`05_output/` 未建）。**

## 现在做到哪儿

| 层 | 文件 | 状态 |
|---|---|---|
| 地图 | `README.md` | ✅ v4：定位 / 核心结构 / 主轴与规格 / 页数预算 20 张 |
| 术语与红线 | `CONTEXT.md` | ✅ v4：三套编号补齐、② 与 ⑤（续）固定说法、禁用词含"上屏标题不得含 harness" |
| **故事线** | `01_storyline/00-storyline-map.md` | ⏳ **v4，待 review**：**19 正文 + 2 停顿 = 21 张 ≈ 28.2 min**；**时长以 03 为准** |
| **证据** | `02_evidence/00-sources.md` | ✅ v4：**4 张来源卡片**（新增 Anthropic 一手）+ 3 条待回源 + 5 条口径红线 |
| **页结构** | `03_outline/00-page-structure-v4.md` | ✅ v4：**21 张**逐页职责 / 时长（**27.5 + 0.7 ≈ 28.2**）/ 上屏元素 / 讲法 |
| **PPT 文案** | `04_drafts/ppt-text-v5.md` | ✅ **v5：唯一上屏文案源**；`### ` 页块 21（P1–P19 + B1/B2），页间 20 条 `---` + 头部后 1 条 |
| **对客手册 · 长版** | `04_drafts/实战手册-v5.md` | ✅ v5：讲义（填空模板 + 打磨循环 + 一页清单） |
| **对客手册 · 页版** | `04_drafts/手册-页版-v5.md` | ✅ v5：**20 页（`### HP1`…`### HP20`）**，一页一个提醒（**20/20 恰好一条 `[必上]`**） |
| 产出 | `05_output/` | ⬜ **未建**（等 talk review 过再渲染 PPTX） |

## 当前定调（未经变更不得漂移）

1. **★ 核心命题**：**`Agent = Model + Harness`**。**模型决定上限；那一圈决定你能拿到多少**；
   那一圈搭对了，它跑起来就不糊涂。**这门手艺叫 harness engineering。**
   出处：Trivedy《The Anatomy of an Agent Harness》，2026-03-10（`02_evidence/00-sources.md` §1）。
   **v5 改口径**：原句"模型你只能选，选完大家都一样"**太满**——"换模型 ≠ 性能不变"一撞就破；
   已按行业侧证据改为"决定上限 / 决定拿到多少"。
2. **★ 那一圈拆成五件事**（按序不许换、不许合并）：**看到什么 / 懂什么 / 会什么 / 怎么做 / 怎么验**。
3. **★ 五种症状有学术底稿**：MAST（NeurIPS 2025，14 种失败模式 / 3 大类）——
   **13 个归位到五件事，1 个未采用**（隐瞒关键信息，多 agent 专属）；**③ 无 MAST 对应**。
   最高频**步骤重复 17.14%**，其次**推理与行动不一致 13.98%**、**用错误假设继续而不澄清 11.65%**。
   **频率数字不上屏**。
4. **症状与五件事一一对应、同号同序**（看到 → 懂 → 会 → 走 → 验）；五件事连续讲完（P7–P13），
   中间不插停顿页。**⑤ 因要回答"那段检查谁写"拆 P12 / P13 两页**（拆页不是合并）。
5. **★ P4 必须回答「等下一代模型不就行了」**——**这一圈不会消失，只会移动**：
   每个组件都编码了一个"模型做不到"的假设。**不回答它，整场必要性立不住。**
   **v4 已回源：依据是 Anthropic 官方工程博客（2026-03-24），一手**。
6. **搭多厚由两句判据决定**（错了赔得起吗 / 多久能发现，P14）——回答"搭多厚"，不是"搭不搭"。
7. **全场最重要的一页 = P12**：**AI 负责干，代码负责查**；**当场命名「代码」**；
   落点 **能让代码查的，别让 AI 自己说**。**会不会写不影响用**（写不用听众写）。
8. **四档降为"这一圈归谁"**（第 1–2 档现成 / 第 3–4 档自己搭），只占 1 页；**不许讲成"越复杂越高级"**。
   P14 与 P15 两条轴由 **P16「两页合起来用」**合成。
9. **harness 上屏两次**（P5 核心命题 / P19 正名），其余各页一律说「**模型外面那一圈**」；
   **上屏标题亦不得含该词**（原候选已剔除）。
10. **编号三套不许撞车**：症状 / 五件事 = `①②③④⑤`；**四档 = 第 N 档**；**判据 = 那两句，不用编号**。
11. **上屏只留作者 / 机构 + 年份**，不留公司名、产品名、工具名；**上屏署名只发生在 P4 / P5**。
12. **本场原则上不引数字**：**P5 的 Top 30→Top 5 默认也不上屏**（无法独立回源 + 公开榜已换代；
    若上屏须标 ⚠️ 厂商自述 + 观测日期）。
13. **不套用其他 talk 的红线**——本场红线在 `CONTEXT.md` 第四 / 六节，是本场自己的。
14. **P2 先给一个场景，再上 P3 的五种症状认领**；认领问法用"**不用说出来**"（房间可能有上级在场）。
15. **P13 必须回答"那段检查谁写"**——现成的 / 找工程同事加一条 / 让 AI 写但你审它检查什么。
16. **页数预算是 21 张**（19 正文 + 2 停顿；v10 用余量加了 P6 主线页）；**宁可加一张轻页，不要在一页上堆**。

## v15 手册对齐 v5 + 补齐 7 处缺口 + CURRENT 收口（2026-09-22，用户定）

> 用户："版本直接调到 **V5** 好不好，就跟 `ppt-text-v5.md` 配合起来了。"
> 另有"我还有没有忘了想的"一问——核出 7 处，**本轮一并补进 v5**（不再滚版本）。

| 项 | 处置 |
|---|---|
| **版本对齐** | `实战手册-v2.md` → **`实战手册-v5.md`**，与 `ppt-text-v5.md` **同版**（两件配套，版本号一起走） |
| **① 补回 talk 的后半场** | v2 换骨架时把「挑一件真事 / 四档（你放多少手）/ 两句判据（你盯多紧）」弄丢了 → 新增 **第 1 步「开摆之前，先把三件事定了」**（放在"摆"的上游），其后步骤顺延 |
| **② 例子补回 3 个** | 附录 B 保留"打磨三轮"演示；**新增附录 C：另外三个场景速查**（对外通知 / 内部资料 / **第 4 档多条线**） |
| **③ 补回"加之前也要减"** | 第 6 步加一段：**上一轮加的东西还在起作用吗**——模型变强后有些规矩要退休（**那一圈会移动**）；只加不减会越堆越厚 |
| **④ 两套名字对上** | 第 2 步开头加一行映射：**工作空间＝看到什么 ｜ 知识＝懂什么 ｜ 能力＝会什么 ｜ 流程＝怎么做 ｜ 规矩与约束＝怎么验** |
| **⑤ 合规那一句** | ① 里加醒目提醒：**摆放之前先问 IT / 合规——哪些资料根本不能进这个工作空间**（客户明细 / 人事 / 未公开）；清单加一行 |
| **⑥ 查不到的错怎么办** | ⑤ 里加：**判断类的错，检查查不到，只能靠"错了多久能发现"兜底**——把它挪到"马上能发现"那一格 |
| **⑦ "什么时候算驾驭住了"** | 第 6 步加验收信号：**你敢只看检查结果，就把它交出去** |
| **⑧ CURRENT 收口** | v4–v14 共 **13 节整体移入 `CURRENT-history.md`**（冷区）；**CURRENT 从 334 行 → 97 行**，只剩 一句话 / 现在做到哪儿 / 当前定调 / 本轮记录 / 下一步 / 待定 / 缺口 |
| **⑨ 清掉过期的缺口** | 原缺口三条其实早已补上（P13 检查例子 / P9 老手坑 / P17 场景），还有一条写着"页数预算已用满 20 张"（现为 21）——**热区文件写错比缺文件更危险**，已按现状重写 |

## v16 手册出「页版」：一页一个提醒，供 PPT Agent（2026-09-22，用户要求）

> 用户："可不可以你理理顺，因为我想把它**变成 PPT**，让人一页一页地去看，然后**一页提醒一个东西**。
> 现在人都懒嘛…… `ppt-text-v5.md` 就组织得比较**容易被 PPT 接收**了，
> 因为我回头用一个 PPT 的 Agent，它很容易理解这个结构……我猜估计会搞个**十几二十页**的样子。"

| 项 | 处置 |
|---|---|
| **新文件** | `04_drafts/手册-页版-v5.md`——**20 页，一页一个提醒**；字段与记号**与 `ppt-text-v5.md` 完全一套**（`L1/L2/L3/[必上]/[可选]/[例]/落点/L4 页脚` + 页间 `---`），**两个文件可喂给同一个 Agent** |
| **为什么另起页版** | 长版手册（`实战手册-v5.md`）是**讲义**，有填空模板、打磨循环、一页清单——**直接切成幻灯片会丢细节**。页版用"候选内容池"写法（`[可选]/[例]` 兜住细节），**长版留着当可发可读的讲义** |
| **20 页怎么分** | 封面 1 ／ 世界观 2（只看五件事 + 只能靠打磨）／ 主线循环 1 ／ **开摆之前 3**（挑真事·定第几档·定盯多紧）／ **五个地方 5** ／ **各加一条 2**（合规 · 查不到的错）／ 跑看补 3 ／ 打磨三轮的样子 1 ／ 驾驭住了 1 ／ 什么时候别用它 1 |
| **防超载** | 每页**只有一个 `[必上]` 提醒，其余进 `[可选]/[例]`**——已核：20 页 × 每页 1 条 `[必上]`；页码 1–20 连续；页块 20 / 分割线 20 |
| **登记** | `CONTEXT` §七 分清了「talk 的渲染来源」与「手册的渲染来源」；`README` 与 `CURRENT` 状态表新增页版一行 |

## v17 三个文件过一遍「中文惯用法」（2026-09-22，用户反馈）

> 用户："这些内容刚才用的时候发现有一个突出的问题，就是**中文的惯用法表达有问题**，
> 要用中文习惯的表达来表达，否则**读起来好别扭**。"

| 项 | 处置 |
|---|---|
| **根因** | v8 之后新写的大批内容（内容池、信噪比、手册长版、手册页版）**没有再过"说人话"那一关**——只有 `ppt-text` 早期部分在 v6 过过 |
| **改的是句式，不是词** | ① **表单腔标签**（"你要摆清楚：／放清楚的标准：／你要摆清楚四件："→"摆什么：／怎么算摆清楚了：／摆四样："）② **单字加引号**（第一件事就是"看"→就是**看**）③ **指代不统一**（哪一格/五格/哪一样混用 → **一律"哪个地方"**）④ **缺虚词**（全堆一个文件夹 → 全堆**在**一个文件夹**里**）⑤ **"是…的"判断句**（不是用来做对的，是用来…的 → 不是为了一次做对，是为了…）⑥ **施动者错位**（引它自己的心理话改用第一人称：它在哪儿？→ **我**在哪儿？）⑦ **僵搭配**（在这里→在这儿／对它就等于不存在→**它就当没有**／得你先把话说到→得你先把话**说明白**）⑧ **连接词过文**（并且→也／以及→再看） |
| **改了 40 处** | `实战手册-v5.md` 22 处 ／ `手册-页版-v5.md` 11 处 ／ `ppt-text-v5.md` 7 处 |
| **固化** | `01-narrative-check.md`「说人话自检」表 **+8 行**（每行一个反面例子→改法）；`CONTEXT` 讲法纪律**第 12 条加严**（把这八类写进纪律，下次直接对着查） |
| **复扫** | 三个文件的旧标记（在这是/就等于/把话说到/放清楚的标准/哪一格/五格/以及/并且）已归零。**但 v18 独立复核又查出成片残留**（含 v17 明令改过却没改净的"（摆清楚四件）"、"就算摆清楚了"×3）——**"扫一遍就归零"这个说法本身不成立**，见 v19 |

## v19 独立复核结果处理——4 条阻断 + 5 条重要 + 6 条小，全部已改（2026-09-22）

> 复核者逐页真读三个文件（约 1200 行），结论：**不能直接交给 PPT Agent**。
> 它还指出一件我没意识到的事：**复核期间我仍在改文件**，它第一遍读到的版本已被我改掉。
> ——**教训：派复核之前先提交、把 hash 给它**（它给的改前 hash：ppt-text `b9c63137` ／ 页版 `7880441c` ／ 长版 `6dd15179`）。

| # | 问题 | 修法 |
|---|---|---|
| **阻断1** | `ppt-text` **分页结构**：停顿页用 `##`，按 `---` 切出 22 块、按 `### P` 只有 19 页 → Agent 会**丢停顿页或把文件头当一页** | 停顿页改 **`### B1 / ### B2`**；INTERFACE 写死切页规则：**页块 = `### `**／`## ` 是分段不是页／头部不是页／页间必有 `---` |
| **阻断2** | **上屏字段里混了 8 处导演提示**（"（挑一个讲，别都讲）"等）→ 会被逐字搬上屏，且与"不含不上屏内容"自相矛盾 | 04 **删 8 处**，等价信息**移入 03 逐页讲法**；立规：**方括号只放优先级记号，其他说明用圆括号** |
| **阻断3** | 页版"**一页一个提醒**"名实不符：19/20 页的 `[必上]` 块含 2–5 条或整表 | **字段重构**：新增 **`提醒 [必上]`（每页恰好 1 条）**；材料降为 **`要点 [必给]`**（可多条可含表）。脚本核验：**20 页 × 每页恰好 1 条 `[必上]`** |
| **阻断4** | **字段不闭合**：`附带 [必上]`、`L3 支撑 [听众会说的三种反驳]` 不在任何字段表；`落点` 一个文件带 `[必上]` 一个不带 | `附带` 补进 04 与 03 字段表；`[听众会说的三种反驳]`→`[可选]`；统一"`结论句/落点/命名/附带` 一律带 `[必上]`"；**把"同一套字段"改准**为"**共用记号约定，字段表各自独立**" |
| **重要5** | 红线：`版本`（上屏）／`遇到 X 就去查 Y`（占位字母）／`（待补）`／`IT`／"强一百倍" | 逐条改：**给客户看的那份**／**这种情况去哪儿查**／去掉"（待补）"／**管技术的或管合规的同事**／**强得多**；含数字的示例文件夹名一并去数字 |
| **重要6** | 术语：长版 `智能体` 未命名先用且含义漂移；手册用 `程序` 而演讲命名 `代码` | 长版首句改"**它**其实只看这么几样东西"；手册统一用 **`代码`** |
| **重要7** | 五格↔五件事的桥只在长版写了一次，页版全篇没有 | 页版 P2 补一行映射 |
| **重要8** | 长版**对客正文泄露内部文件名** `ppt-text-v5.md` | 改"与那场分享的幻灯片同版" |
| **重要9** | 惯用法成片残留（13 处），含 v17 明令改过却没改净的 | 逐条改；**并承认上一轮"扫一遍就归零"的说法不成立** |
| **小10–15** | 指代/事实错位/语义不接/问句 L1/断链/措辞不齐 | `上一页说的`→`前面说的`；`这一圈有名字：公式`→`这一圈有个名字，叫 harness；写成一句话就是…`；**P18 病灶与检查项对齐**；`同时有几个在跑` 三处统一；CONTEXT 指针改指 `01-narrative-check` 第三节 |

**复核者同时确认已通过的**：页脚格式、页间 `---`、五个术语的命名页、三轮/四轮已统一、P16 顺序矛盾已不成立。
**复核者另给的一条交付纪律**：**先冻结 hash 再复核**（否则结论会随改动过期）。

## v20 第二轮复核——3 条阻断 + 7 条重要 + 10 条小，全部已改（2026-09-22）

> **先冻结 hash 再派复核**（v19 的教训已执行）。复核者核对 **7 个文件 hash 7/7 相符**，
> 仅 `03_outline` 在复核期间被我改过一版（`716f53af` → `07d75f0c`），它按新版继续。
> **结论：v19 的 4 条阻断经复核"确实修好了"**（不是名义修好）。新查出下面这些。

| # | 问题 | 修法 |
|---|---|---|
| **阻断1** | `**…就把哪一项交给**代码****` —— **嵌套加粗写错，会原样渲染出 `****`** | 页版 HP13、长版 §⑤ 两处改为 `**…交给代码**`；**顺手把 `代码` 命名补全**（长版/页版此前未命名先用） |
| **阻断2** | `ppt-text` 上屏正文里混入**讲者指令**「（说出来听众都见过）」 | 04 删；等价信息**移入 03 的 P5 讲法** |
| **阻断3** | 文件头**自述不实**：①"不含任何不上屏内容"（但字段表本身不是上屏内容）②"落点一律带 `[必上]`"（119/398 两处不带，398 还落在 `[可选]` 块里） | ① 改准为"**不含任何给讲者的不上屏内容**"，并声明 `版式` 是渲染指令、`讲者/场次/日期` 是空槽 ② **两处"落点"从内容项升级为真正的 `落点 [必上]` 字段** |
| **重要4** | 长版是**讲义**，但文件里没声明它**不是渲染输入** | 长版开头补一行：**这是讲义版**——可打印、可传阅；投屏用的页版另发 |
| **重要5** | 未声明字段：`脚注（小字）`、`（大字）/（小字）` 字号指令、`讲者/场次/日期` 空槽；且正文里出现**内部页号**（"第 13 页"、"P2 的场景图"） | 字段/规则**全部写进 ppt-text 与 CONTEXT 的接口节**；两处内部页号改为**"前面讲的那三条路"／"开头那个场景的图"** |
| **重要6** | 长版是**对客文本**，却带版本号（标题 `v5`、"版本说明：同版"） | 标题去掉 `v5`；"版本说明"整行换成讲义定位那句 |
| **重要7** | 手册**先用 `代码` 后命名**（talk 的命名页在 P12，手册自成一份） | 手册两处补命名；并在 `CONTEXT` **声明手册自带第二套叫法**（五个地方 ↔ 演讲的五件事） |
| **重要8** | ①五格↔五件事**映射写错**；②`ppt-text` 上屏写了**"最高频"**，而 03 明令**只口播** | ① 映射逐格重写为"工作空间就是看到什么…"② 上屏删"最高频"三字（03 讲法里保留） |
| **重要9** | **页号命名空间会撞**：演讲 `P1…P19`、手册也 `P1…P20` | **手册页版改 `### HP1`…`### HP20`**；`CONTEXT` 写明两套前缀不许撞、正文提"第几页"须指明是哪个件 |
| **重要10** | `03` / `storyline` 引 B2 小字是"**以及**它坏了"，04 是"**再看**它坏了"（v17 定的改法没同步到上游） | 上游两处同步为"**再看它坏了**" |
| **小11–20** | 312 个 ASCII 双引号（`"`）／全角半角斜杠混用／`≠ ＝ ×` 当连接词／`①–⑤`／`停在 第 1 档`多空格／`闭环`／指代混用（五样/五个抽屉/那边）／`（一／多）`／`只用看`／`看起来还行`／`③（能力与圈）`／`一个人把稿子推出去`／P8 条目重复／页版 P1 提醒与副标题同句／"前面那场分享" | 逐条改：**引号 312 个全成对转中文引号（0 奇数行）**；**斜杠统一 `／`**；**数学符号全换中文（`×`→"但/而且"）**；符号 `①–⑤`→`①②③④⑤`；**指代一律"五个地方"**；P8 删掉与 `[必给]/[例]` 重复的那条；页版 P1 提醒换成**它自己的命题**；**附录 C 里"第 1 档 + 赔得起×很久才发现"与 P14 四格自相矛盾——删掉附录的 2×2 标注，附录只保留"第 N 档"** |
| **小（storyline 时长漂移）** | storyline 合计 **27.0 / 27.7 min**、五件事 **7 页 / 11 min**、短场次 **14 页 / 23.0 min**——与 03 的 **27.5 / 28.2**、**8 页 / 11.5**、**15 页 / 24.0** 全对不上（且 19−4＝15 页，不是 14） | **以 03 为准全部改齐**，并在 storyline 里写死"**时长与删法以 03 为准**" |

**这轮自己被复核纠正的一件事**：`CONTEXT` 我原写"手册页版另有 `脚注` 字段"——**脚注其实是演讲件的字段**，
页版不用字号指令。已改准。

**复核给出的结构性建议（未改，登记为待定）**：页版 ⑤ 是否把"检查 / 验收"拆成两格。

## 下一步

1. **★ review `01_storyline/00-storyline-map.md`（v4）**。重点五处：
   - **P2 那个场景**——像不像听众真会遇到的事？（全场地基）
   - **P4 的反驳回应**——现在有一手源，"这一圈不会消失，只会移动"够不够有力？
   - **五件事的命名与顺序**（看到 / 懂 / 会 / 走 / 验）——对不对？
   - **P12 + P13**——"AI 负责干、代码负责查"+"那段检查谁写"，是不是你要的说法？
   - **21 张 / 28 min 的页表**——预算是否刚好，还是要按"压到 ≈24 min 的删法"再收。
2. review 过了再定：上屏标题三选一 / Top 30→Top 5 要不要上屏 / 例子要不要出现平台名。
3. **review 通过后**才渲染 `05_output/`（PPTX）；`04_drafts/ppt-text-v5.md` 已是渲染输入。

## 待定（等你拍板）

| # | 待定 | 当前口径 |
|---|---|---|
| 1 | 上屏标题 | **v5 已定**：**「它不糊涂，是因为外面那一圈搭对了」**（另两条留备选，一句话可换） |
| 2 | P5 的旁证 | **v5 起口播旁证改用行业侧方向**（不点产品名、不报数字）；原 Top 30 → Top 5 降为备选（无法独立回源 + 公开榜已换代，**数字一律不上屏**） |
| 3 | 例子要不要出现真实平台名 | 不出现 |
| 4 | 是否配一份图文版（同内容，可发可读） | **v5 定：暂不做**——先 review 内容；要发再按 04 稿抽一版 |
| 5 | 是否另存一份口播讲稿 | **v5 定：暂不做**——04 已有上屏文案 + 导演提示；渲染前再决定要不要单独出讲稿 |

## 缺口

- **五种症状还是"学术分类的中文翻译"**——MAST 给的是分类，**到现在没找过一个真听众过一遍**。
  建议：找 2–3 个目标听众（文职岗）念一遍 P3，把不点头的那条换掉。**这是全场地基。**
- **口播讲稿还没有**——`03_outline` 只有导播提示，**没有能照着念的稿**；28 分钟的场子建议补一份。
- **手册 ⑤ 要不要拆**——"检查 / 验收"是否单独成一格（talk 的 ⑤ 是支点页，手册里只是一条）。**待拍板。**
- **待回源 3 条**（`02_evidence/00-sources.md` §6）——本场当前不依赖。
- **仓库其他目录存在同类日期偏差**（文件写 2026-09-21、git 提交 2026-09-22，如 `03_practice/harness_governance/`）。
  按"不跨层自行统一"的纪律未动，需要时由用户决定是否做一次仓库级对齐。

---

> **历轮记录**：本目录 2026-09-22 开张，同日走完 **v0 → v15**。
> **v4–v14 的修复纪要已整体移入 `CURRENT-history.md`**（冷区，默认不读）；v3 轮次也在那里。
> **已提交**：`1d1b314`（v2）、`0a59258`（v3）、`82c8f35`（v4）……本轮见 `git log`。
