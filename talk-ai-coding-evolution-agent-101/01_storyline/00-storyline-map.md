# 故事线总图 v2（待 review）

> **v2 相对 v1 的根本变化（2026-09-21 用户定调）**：
> 1. **核心命题升为 `Agent = Model + Harness`**——模型你只能选，**差别在模型外面那一圈**；
>    那一圈搭对了，它跑起来就不糊涂。**这个学科叫 harness engineering。**
> 2. **那一圈拆成五件事**（用户口述，替换 v1 的"规矩/现行/边界/对错/记录"）：
>    **看到什么 / 懂什么 / 会什么 / 怎么做 / 怎么验**。
> 3. **五种症状有了学术底稿**：MAST（NeurIPS 2025）的 14 种失败模式按这五件事归位，见下。
> 4. **四档降为"这一圈归谁"**（①② 现成 / ③④ 自己搭），不再占主体篇幅。
>
> 术语与禁用词以 `../CONTEXT.md` 为唯一权威；证据与来源见 `../02_evidence/00-sources.md`。

---

## 题目

**上屏标题（候选，待定）**：

1. **Agent = Model + Harness：同一个模型，换一圈，就是两套系统**
2. 模型你只能选，外面那一圈你能搭
3. 它不糊涂，是因为外面那一圈搭对了

**副标题固定**：**搭对了，它跑起来就不糊涂。**

## 一句话主线

**Agent = Model + Harness。模型你只能选，选完大家都一样；真正决定它好不好用的，
是模型外面那一圈。那一圈搭对了，它跑起来就不糊涂——这门手艺叫 harness engineering。**

---

## 全场主轴：只追一个问题

> **同一个模型，为什么在别人手里好用，在你手里糊涂？**

**为什么是这个问题**：

1. **听众已经在被它折磨**——它忘了你说过的话、不懂你的业务还不问、反复绕圈、说做完了其实没做完。
2. **它把答案逼到唯一的地方**：不是模型不行（模型你也没法改），是**模型外面那一圈没搭**。
3. **它天然推出五件事**：那一圈要搭的，就是让它**看到什么 / 懂什么 / 会什么 / 怎么做 / 怎么验**。

---

## 第一段：五种症状（听众先认领）

> **纪律**：这五条要**让听众先认领**（"这几条你中过哪一条？"），不要念完就往下走。
> 底下括号里是 MAST 的对应失败模式与出现频率——**只作本场的底稿，不上屏**（本场原则上不引数字）。

| 症状（听众的话） | 它缺的是 | MAST 底稿（不上屏） |
|---|---|---|
| 它忘了你说过的话；拿到的是旧版本 | **该看的它没看到** | 上下文丢失 3.33%；忽略其他输入 0.17%；对话被意外重置 2.33% |
| 它按通用套路干，不懂你们这行的规矩，**还不问，自己猜** | **该懂的它不懂** | 没遵守任务要求 10.98%；**用错误假设继续而不澄清 11.65%** |
| 它干不了（够不着你的系统）；用了笨办法；**碰了不该碰的** | **该会的它不会** | 工具/环境缺失（Trivedy 的 harness 组件面） |
| 它**反复绕圈**；跑偏；提前收工；**说的和做的不是一回事** | **该怎么走它不知道** | **步骤重复 17.14%（最高频）**；推理与行动不一致 13.98%；任务脱轨 7.15%；未识别任务完成 9.82% |
| 它说做完了其实没做完；检查只做了表面；**自己评自己总说好** | **该怎么验没人管** | 提前终止 7.82%；未验证/验证不完整 6.82%；验证错误 6.66% |

> **来源**：MAST（Multi-Agent System Failure Taxonomy），UC Berkeley 等，
> [arXiv 2503.13657](https://ar5iv.labs.arxiv.org/html/2503.13657)，NeurIPS 2025 Datasets & Benchmarks；
> 200+ 条执行轨迹、6 位专家标注、Cohen's Kappa 0.88。**证据强度：一手（同行评审）**。
> 三条结构性结论可直接引用：① 失败**主要来自系统设计，不是模型能力不足**；
> ② 有验证者也不够——**现有验证大多只做表面检查**；③ 加验证步骤使 ChatDev 成功率 +15.6%。

---

## 第二段：核心命题（为什么不在模型）

### 不是模型不行

你**只能选**模型，选完就没法改了。同一个模型，在不同人手里表现不一样——
所以差别不在模型，在**模型外面那一圈**。

### `Agent = Model + Harness`

> **"If you're not the model, you're the harness."** —— Viv Trivedy

- **模型**：给能力。你只能选，控制不了。
- **harness（模型外面那一圈）**：给可靠性。**你拥有它、你能搭它、它决定它跑起来糊不糊涂。**
- 一句话：**模型给智能，那一圈让智能真的能干活。**

**旁证（可上屏，标注来源）**：同一个模型，**只换 harness**，在 Terminal Bench 2.0 上从 Top 30 进到 Top 5。
（⚠️ 厂商自述，LangChain 2026-03-10；本场只作方向性信号。）

**命名纪律**：`harness` 这个词**只在本页与收尾正名出现**；其余各页一律说「**模型外面那一圈**」。
理由与例外见 `../CONTEXT.md` 第二节 2.2。

---

## 第三段：那一圈要搭的五件事（本场主体）

> **五件事按"它需要什么"排**：先看到 → 再懂 → 再会 → 再按路走 → 最后被验证。
> **每件事固定四段**：症状 → 它是什么 → 你要做什么 → 一句话判词。

### ① 让它看到该看的（上下文）

- **症状**：它忘了你说过的话；它拿到的是旧版本；关键那份它没看见
- **它是什么**：模型能用的，**只有此刻放在它眼前的东西**——你给它的，就是它知道的全部
- **你要做什么**：
  - 把该给的给到位（规矩、背景、这次要用的资料）
  - **告诉它哪份是最新的**——它分不清"现在这么做"和"以前这么做"
  - 别一次塞太多：**塞得越满，中间的东西越容易被漏掉**
- **判词**：**你以为它知道，其实它没看见。**

### ② 让它懂你的业务（把老手脑子里的东西写出来）

- **症状**：它按通用套路干；不懂你们这行的规矩和黑话；**不懂也不问，自己猜**
- **它是什么**：模型的知识是**通用的**；你们这行真正管用的东西，大多在**老手的脑子里**，没写下来
- **你要做什么**：**把隐性知识显性化**——不是写手册，是写"这件事我们这儿怎么做 / 为什么这么做 /
  什么情况不能这么做"
- **判据**：**老手一句话能说清的那个坑，写下来它就不会再踩**
- **判词**：**隐性知识不写出来，它就只能猜。**

### ③ 让它有该有的能力（工具与边界）

- **症状**：它干不了（够不着你的系统）；用了笨办法；**碰了不该碰的**
- **它是什么**：模型只会输出文字；**能做事的是你给它的工具**
- **你要做什么**：
  - 给它够得着的工具（查资料、读表、发消息）
  - **同时划圈**：它能读什么、能对谁发东西、能不能花钱
- **判词**：**给能力的时候，顺手把圈划上。**

### ④ 让它按对的步骤走（工作流）

- **症状**：**它反复绕圈**（最高频）；跑偏；提前收工；说的和做的不是一回事
- **它是什么**：你给一个目标，它自己决定下一步——**步骤对不对，它自己不知道**
- **你要做什么**：
  - 把关键步骤写下来：先做什么、后做什么、**什么时候必须停下来问你**
  - 长任务要能接着上一段继续（不然每换一段就重来）
- **判词**：**它不迷路，是因为你先把路画出来了。**

### ⑤ 让它能被验证（传统代码把关）★ 全场最重要的一页

- **症状**：它说做完了其实没做完；检查只做了表面；**自己评自己总说好**
- **它是什么**：**AI 的判断每次可能不一样**——所以它**不能当最后的对错判官**
- **你要做什么**：**AI 负责干，传统代码负责查**
  - 代码每次跑的结果都一样，所以能替你把「对不对」这一步**定死**
  - **会不会写不影响你用它**——写不用你写
  - 你要知道该要求什么：一段跑起来**会给出明确对错**的检查 / **每次都一样** / **卡在最容易出错的那一步**
- **附带一条**：**干活的和验收的不能是同一个**——自己评自己，一定说好
- **判词**：**能让代码查的，别让 AI 自己说。**

> **MAST 直接支持这一页**：验证类失败占 21.3%，且"有验证者也不够"——
> 现有验证大多只做表面检查。**加一层验证，ChatDev 成功率 +15.6%。**

---

## 第四段：这一圈归谁（四档）

> **四档不再占主体篇幅**，只回答一件事：**这一圈是现成的，还是你自己搭。**
> 定位的两个问题：**下一步谁定**（你 / 它）× **同时有几个在跑**（一个 / 多个）。

### ①② 这一圈是现成的

- **① 你问一句，它答一句**：你自己就是那一圈。全程在场，代价最小。
- **② 你定好步骤，它照着走**：平台把圈搭好了，但**步骤和检查要你写进去**。
- **风险**：一步烂，后面全烂，而且**错得很整齐，看起来很像对的**。
- **落点**：**大多数听众停在这里就够——停在 ① 完全没问题。**

### ③④ 这一圈得你自己搭

- **③ 你给个目标，它自己跑**：你只在开头和结尾出现，中间全靠那一圈。
- **④ 你定好分工，它们自己配合**：还要多定一件事——**谁验收**。
- **风险**：跑偏没人知道 / 碰了不该碰的 / 该停不停 / 互相甩锅。
- **关键判词**：
  - ③ **它自己能验证的活，才敢让它长跑。**
  - ④ **多一个执行者之前，先问「这一步是不是真的需要另一个脑子」。**
- **落点**：**越往下走，五件事越不能省。**

---

## 第五段：收尾

### 什么时候别折腾（对这批听众最重要的一页）

- 一句话能说清的事，别写流程
- 一周做一次的事，别搭自动化
- **还没想清楚"什么样算做对"，别交给它自己跑**
- 先跑最简单的，不够再加。加之前先问：**谁维护它？它坏了谁发现？**

### 今天就能做的两件事

1. 挑一件你正在让 AI 干的事，对一遍五件事——**哪一件你根本没做？**
2. 如果是 ③ 或 ④，先把这三条写下来：**它能碰什么**、**什么情况必须停下来问你**、
   **有没有一段代码能替它说"这步没问题"**

### 回到开场 + 正名

**你现在能答出"它为什么糊涂"了吗？答不出的那一条，就是你那一圈缺的东西。**
**这一圈有名字**——`Agent = Model + Harness`，搭它的这门手艺叫 **harness engineering**。

---

## 页表 v2（15 页正文 + 2 张停顿页 ≈ 27 min）

> **L1 = 这一页在答哪一步**，不是结论句。

### 开场（2 页 / 3 min）

| 页 | 这一页在答什么 | min | 必讲要点 |
|---|---|---:|---|
| P1 | — | 0.5 | 标题 + 副标题「搭对了，它跑起来就不糊涂」 |
| P2 | 它糊涂起来是这样的 | 2.5 | 五种症状。**必须让听众先认领**（"你中过哪一条"） |

### 核心命题（2 页 / 4 min）

| 页 | 这一页在答什么 | min | 必讲要点 |
|---|---|---:|---|
| P3 | 不是模型不行 | 1.5 | 模型你只能选；同一个模型在不同人手里不一样 |
| P4 | `Agent = Model + Harness` | 2.5 | 模型给能力、那一圈给可靠性；**"If you're not the model, you're the harness."**；Top 30→Top 5 旁证（⚠️ 厂商自述） |

### 五件事（6 页 / 11.5 min）—— 连续讲完，中间不插停顿页

| 页 | 这一页在答哪一步 | min | 必讲要点 |
|---|---|---:|---|
| P5 | 那一圈要搭的五件事 | 1.5 | 只出五件事的名字与顺序，细节留给下面五页 |
| P6 | ① 让它看到该看的 | 2 | 你给它的就是它知道的全部；**告诉它哪份是最新的**；塞得越满漏得越多 |
| P7 | ② 让它懂你的业务 | 2 | **隐性知识显性化**：把老手脑子里的坑写下来。判词：不写出来它就只能猜 |
| P8 | ③ 让它有该有的能力 | 1.5 | 能做事的是工具；**给能力时顺手把圈划上** |
| P9 | ④ 让它按对的步骤走 | 2 | 步骤重复是最高频失败；写步骤 + **停手条件** |
| P10 | ⑤ 让它能被验证 ★ | 2.5 | **全场最重要的一页**。AI 负责干，传统代码负责查；**能让代码查的，别让 AI 自己说** |

### 停顿页 B1（P10 之后 · 顿悟型）

大字：**同一个模型，换一圈，就是两套系统。**
小字：**它糊涂，不是它笨，是那一圈没搭。**
（收五件事、启"这一圈归谁"）

### 四档（2 页 / 3.5 min）

| 页 | 这一页在答哪一步 | min | 必讲要点 |
|---|---|---:|---|
| P11 | ①② 这一圈是现成的 | 1.5 | 你自己就是那一圈；② 要写进步骤和检查。**停在 ① 完全没问题** |
| P12 | ③④ 这一圈得你自己搭 | 2 | ③ 判词：**它自己能验证的活，才敢让它长跑**；④ 判词：**先问这一步是不是真的需要另一个脑子** |

### 停顿页 B2（P12 之后 · 反思型）

大字：**它说它做好了——这句话值多少？**
小字：**值多少，取决于有没有一段代码能替它证明。**
（收四档、启收尾）

### 收尾（3 页 / 4 min）

| 页 | 这一页在答哪一步 | min | 必讲要点 |
|---|---|---:|---|
| P13 | 什么时候别折腾 | 1.5 | 四条"先别"。**对这批听众，这一页比四档更重要** |
| P14 | 今天就能做的两件事 | 1.5 | 对一遍五件事 / 写下"能碰什么 + 必须停下来问 + 有没有代码替它证明" |
| P15 | 回到开场 + 正名 | 1 | 答不出的那条就是缺的那件；**harness engineering** 正名 |

**合计**：正文 26 min + 停顿页约 0.7 min ≈ **27 min**（20–30 区间内）。
**压到 20 min 的删法**：P5 并入 P4、P8 并入 P9、P14 并入 P15 → 12 页正文 + 1 张停顿页。

---

## 叙事检查（v2 自查）

1. **全场只有一条线**：P2 症状 → P3 不在模型 → P4 在那一圈 → P5–P10 那一圈的五件事 →
   P11–P12 这一圈归谁 → P13–P15 收尾。每一页都是上一页的下一步。
2. **五件事与五种症状一一对应**，顺序也一致（看到 → 懂 → 会 → 走 → 验）。
   **任何一页讲不出对应的症状，就是零增量页。**
3. **`Agent = Model + Harness` 在 P4 给**（症状已在 P2 建立动机）；**harness 这个词全场只在 P4 与 P15 出现**。
4. **五件事连续讲完**，中间不插停顿页（继承母版纪律）。
5. **P10 是全场支点**：把"代码"从技能门槛翻成**质量控制工具**。讲法纪律见 `../CONTEXT.md` 第五节。
6. **四档压缩但不删**：它回答"这一圈归谁"，是听众对号入座的地方；**不许讲成"越复杂越高级"**。
7. **收尾必须收着讲**：P13「什么时候别折腾」不能省，否则本场就是劝人上自动化。
8. **本场原则上不引数字**：MAST 的频率只作底稿，不上屏；唯一的例外是 P4 的 Top 30→Top 5（须标 ⚠️ 厂商自述）。
9. **零技术术语**：上屏与讲稿全部过 `../CONTEXT.md` 第四节禁用词表。

## 素材与证据指针

**外部来源（本场新引入，来源卡片见 `../02_evidence/00-sources.md`）**：

| 用途 | 来源 | 证据强度 |
|---|---|---|
| 核心命题 `Agent = Model + Harness`、harness 定义与组件面、五件事的工程对应 | Trivedy《The Anatomy of an Agent Harness》，LangChain，2026-03-10 | 一手（术语提出者本人）；Top 30→Top 5 为 ⚠️ 厂商自述 |
| 五种症状的分类底稿（14 失败模式 / 3 大类） | MAST，arXiv 2503.13657，NeurIPS 2025 | **一手（同行评审）** |
| 棘轮律（一条规矩 = 一次真实踩过的坑）、验证者分离、"每个组件都编码了一个模型做不到的假设" | Osmani《Agent Harness Engineering》，2026-04-19 | 趋势级（实践者综述） |

**仓库内上游（翻译与重组，不搬结构）**：

| 讲什么 | 去哪取 |
|---|---|
| 七缺口诊断轴（"糊涂 / 乱来"的出处） | `../../03_practice/harness_governance/result/backbone.md` §0.2 |
| 棘轮律、现行共识唯一 home | `result/backbone.md` §1 宪法 2、§2 工件 4/5、§4 节奏 1；`result/manual.md` §2、§7 |
| **传统代码做质量控制（P10）** | `result/backbone.md` §3 回路 1、`result/manual.md` §6；母版 P15/P16（确定性 vs 概率性） |
| 干活的和验收的不能是同一个 | `result/backbone.md` §3 回路 5（evaluator 必须外置） |
| 划圈 / 停手 / 留痕 | 母版 P14–P19（圈住 / 拦住 / 看清）；`result/backbone.md` §5-7 |
| **反过度工程（P13）** | `result/manual.md` §12、`result/backbone.md` §5-5 |
| 「两类程序」的原始推导 | `../../talk-ai-coding-evolution-harness/01_storyline/00-storyline-map.md` |

## 待定（见 `../CURRENT.md`）

- 上屏标题三选一
- P4 的 Top 30→Top 5 旁证**要不要上屏**（当前口径：上屏并标 ⚠️ 厂商自述；也可整页不引数字）
- 例子要不要出现真实平台名（当前口径：**不出现**）
- 是否配一份图文版（同内容，可发可读）
