# Agentic 开发者洞察汇编：分流入口

> 这里收纳的是“很有价值，但不是 Fable 5 专属”的 agentic 样本。
> 只要方法、工作流、上下文工程、harness 工程足够硬，就可以放到这里。
> 找来源时，先参考 `../fable5/fable5_source_pool_map.md`，再决定是进这里还是进 `fable5`。

---

## 当前分流原则

- 明确是 `Fable 5` 用后感、用法、组织变化的，留在 `../fable5/`
- 不是 `Fable 5` 专属，但 agentic 价值很高的，放到 `./`（本目录 `agentic/`）
- 不追只有宣传词、没有 workflow 细节的材料
- 仍然优先保留一手原文和可复核来源

## 当前状态

- 目录已留出，后续遇到高价值但非 Fable 5 的人物/公司时，直接按 `run_公司_名字/` 落进去。
