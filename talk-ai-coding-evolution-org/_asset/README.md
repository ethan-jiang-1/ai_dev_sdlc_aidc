# `_asset/` —— 视觉资产（模板待定）

> 用户明确：**PPT 风格不限制，按内容找到最配合的**。本目录暂不放固定模板；到 PPT 生产阶段（`05_output/`）
> 再按成稿内容选模板，并把选型理由回写到这里。

## 选型原则（备忘，待成稿后细化）

- 内容尚未稳定（v0.2），现在不锁定模板——先推敲故事线，再让模板服务内容，而非反过来。
- **不预设** `-opc` 的 CLAWTIME 模板：那是 OPC 航海主题的视觉；本 talk 是 AI-Native 研发组织 / 全 SDLC 转型主题，基调不同。
- **视觉主线候选**（按内容应优先服务这条线）：
  1. **六阶段 loop 图**（线性 → 循环，AI 嵌在每个点）——承载"AI-native SDLC = loop"主论点。
  2. **工件链 / 审计链**（`intent → spec → plan → diff → PR → incident` 首尾相接）——承载"工件链即审计链"贯穿线。
  3. **harness 三层**（圈住 / 拦住 / 看清 → managed settings / hooks / sandbox / evals / observability）——承载深轴。
- 候选风格方向（待按内容取舍，不限于此）：
  1. **企业治理 / 报告风**：中性白底、克制配色、强层级与表格——契合研发决策者 + 合规听众的预期。
  2. **平台工程 / 架构图风**：以六阶段 loop + 工件链为主视觉的图表达人风格，突出"全链路转型"。
  3. **流程 / loop 对比风**：围绕"线性 vs 循环"、"传统 vs AI-native"的对比版面，强化每阶段的转变张力。
- 决定依据优先级：**内容主线（六阶段 loop + 工件链审计链）→ 听众预期（研发决策者）→ 视觉层级与留白 → 模板保真度**。
