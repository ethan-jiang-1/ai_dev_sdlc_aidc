# PPT 生产素材 v2 · OPC 航海指南（23 页 · 独立完整版 · 文字定稿，按版面取舍）

> **本文件是给 AI Agent 的唯一输入。文字全部定稿，Agent 只负责排版、配色，以及在「候选上屏素材」池里做取舍。**
> 每页分三块：
> - **核心上屏**：必上屏，逐字照抄，不增删改写。
> - **候选上屏素材**：以下内容可以考虑进入 PPT 页面，按版面选用——原则是**宁多勿少**，让每页尽量丰满；挑出来的条目逐字照抄，不自己新造文案。
> - **背景（不上屏）**：讲者备注与口径，只放 speaker notes，绝不上页。

---

## 0. 使用铁律（Agent 必读，逐条遵守）

1. **核心上屏锁死**：每页「核心上屏」是必上屏内容，**逐字照抄**，不增删、不改写、不换词、不重组、不合并或拆分句子。**核心是每页骨架，绝不因候选素材而删减。**
2. **候选上屏素材是取舍池，不是可删空**：每页「候选上屏素材」明确告知——**这些信息可以考虑进入 PPT 页面**。原则**宁多勿少**：版面放得下就全上；放不下时按「标题 > 金句/强调 > 正文 > 附注」的优先级裁掉低优先级条目。挑出来的条目仍逐字照抄，不生产任何新文案；**金句是加料，不替代核心**。
3. **「背景（不上屏）」一律不进 slide**：标注「背景（不上屏）」的内容只放 speaker notes，或直接忽略，绝不出现在页面上。
4. **层级规则**：每页文字括号内是位置 + 层级说明，例如「顶部居中，最大字号」；同一页多个元素按标注的字号大小区分主次。
5. **页数**：P1–P23 共 23 页，顺序固定，不增页、不并页、不删页。

---

## 0.1 这是什么（背景，不用上屏）

- 一场 **45 分钟中文演讲**（硬上限 50 min），题目《**OPC 航海指南：OPC 与 Harness，AI 协作之道**》。
- **OPC = One Person Company（一人公司）**。主题：一个人开公司，要把 AI Coding 掌握到多深？依赖现成还是自己做？
- 听众：懂 AI coding 的程序员 / 产品经理 / 投资人。基调：通俗易懂、抓要害、引发共鸣。

## 0.2 核心立场（转场逻辑靠它，不用上屏）

1. AI Coding 工程注意力五级跳迁：**Prompt → Context → Harness → Loop Engineering → Graph Engineering**。
2. 驱动机制：模型越强，单次交互边际收益越低，杠杆点越往上移；**叠加，不是替代**。
3. 五层讲完，**根本还是 harness**（Agent = Model + Harness，harness 是承重墙）。
4. harness 这一格：**固定 vs 灵活**。立场：不是"别用现成"，是"别只能现成"。
5. 落点：**DSH（DeepSeek Harness）**；金句 **装自己的，借现成的**。

## 0.3 五层定义（备用，供你理解，不用上屏）

| 层 | 一句话定义 | 人的角色 |
|---|---|---|
| Prompt engineering | 把"我该对模型说什么"工程化——提示词是行为控制面，不是系统本身 | 说话者 |
| Context engineering | 把"模型此刻该看到什么"工程化 | 策展者 |
| Harness engineering | 把"模型在什么系统里跑"工程化——权限、沙箱、工具、验证、trace、CI | 环境工程师 |
| Loop engineering | 把"反馈"连成循环——模型越强越能"知错改错"，反馈得当就不断输出更好结果 | 控制流作者 |
| Graph engineering | 把"多个 agent 怎么被编排"工程化——用 workflow / DAG 控制乱发挥，换可靠结果 | 编排者 |

## 0.4 关键人物与金句（署名务必正确）

- Boris Cherny（Anthropic Claude Code 负责人）："I don't prompt Claude anymore. My job is to write loops."
- Birgitta Böckeler（Thoughtworks）："Agent = Model + Harness."
- Addy Osmani（Google Cloud AI Director）："Loop engineering is replacing yourself as the person who prompts the agent."；"Loop sits one floor above the harness."
- Macedo（arXiv）："The harness supplies the engine; loop engineering writes the pilot."
- Woliveiras："A loop can be wrong in ways that are subtle, expensive, and hard to detect."
- Steinberger："designing loops that prompt your agents"（Graph 引爆帖作者，口径：260 万浏览）。
- Walden Yan：一年内从「别建多 agent」改口「有些 setup 真能用」（人机互动在变的旁证）。
- Anthropic："The agent has a prompt, a Bash Tool, and an Edit Tool."；"Start with the simplest viable system."
- GraphARC：模型提议工作图，确定性 checker 放行或拒绝，只有被放行的才执行。
- DSH："No privileged core to patch: you extend dsh by mounting a plugin beside the others."；"Agents follow enforced gates far more reliably than prose conventions."；"Each fact has one home."

## 0.5 关键数字（保持一致）

- METR：50% 可靠性任务长度 ≈ 每 7 个月翻倍
- Claude Code 沙箱：文件 + 网络双重隔离，减少 **84%** 权限提示
- Steinberger Graph 帖：**260 万**浏览 / 2 天（不是 2.6 亿）
- Loop Engineering 定义到被 Graph Engineering 接棒：约 **6 周**
- 2026 CVE：AWS / Google / Vercel（CVE-2026-18830 / 18236 / 64650 / 64651），执行时授权缺失
- DSH 挂 vendor：**15 行 YAML**

## 0.6 术语规范（上屏英文，不译）

上屏保留英文：harness、context rot、Guides / Sensors、computational / inferential、authorize at execution not at generation、capability seam、adapter、vendor、provenance、trace、MCP、RAG、CI。中文口播对应：引导/感知、确定性/概率性、执行时授权、能力接缝、适配器、供应商、溯源、链路。sandbox 上屏用「沙箱」。

## 0.7 风格（你只在这件事上有自由度）

- 配色：深色科技风或白底克制风，全篇统一；P14 用红色调；P21、P22 可做深色大字页。
- 字号层级：标题 > 金句/强调 > 正文；同一页主次按每页标注。
- 视觉主线：航海意象（封面）→ 五层楼图 → 闭环控制图 → 三动词卡片 → 装/借双向图。
- **丰满度**：候选上屏素材尽量上全；页面元素多时用卡片/两栏/标签分块，不要堆成一段长文。

---

### P1 · 封面（0.5 min）

**布局**：整页居中，纵向排布（主标题 / 副题 / 一行小字 / 候选定位行）。

**核心上屏**
- 主标题：**OPC 航海指南**（居中，全场最大字号）
- 副题：OPC 与 Harness，AI 协作之道（居中，次大字）
- 底部一行：一个人，AI Coding 要掌握到多深？（居中，小字）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 定位行：不是工具教程，是讲一个人该站在哪一层（副题下或底部，小字）
- 补充问句：用现成，还是自己做？（可与底部一行并排或上下排）
- 意象标签：海图 / 罗盘 / 灯塔，一条向上的航线（做背景视觉，不占文字层级）

**背景（不上屏，只进 speaker notes）**
- 开场白：今天不讲工具教程，讲一条线索——这两年 AI Coding 的工程注意力怎么一层层往上走，以及一个人（你）该站在哪一层。
- 转场：先讲为什么这个话题跟每个人都有关系。

---

### P2 · 钩子（1.5 min）

**布局**：顶部标题；中部一条横向时间轴（三个时间点）；中部角色阶梯；下部一个引文块；底部一行小字。

**核心上屏**
- 标题：两年，五任角色（顶部居中，本页最大字号）
- 副题：你不是在追新工具，你是在被模型往上推（标题下，小字）
- 时间轴三点（左→右）：
  - 2024 说话者：打磨 prompt
  - 2025 策展者：审代码、喂 context
  - 2026 环境工程师 → 控制流作者 → 编排者
- 引文块："I don't prompt Claude anymore. My job is to write loops." —— Boris Cherny（居中，醒目）
- 底部一行：你不是在追新工具，你是在被模型往上推。（小字）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 角色阶梯五格（一句话各一）：说话者（写 prompt）→ 策展者（喂 context）→ 环境工程师（搭 harness）→ 控制流作者（写 loop）→ 编排者（排 graph）
- 旁证金句："designing loops that prompt your agents" —— Steinberger
- 旁证：Walden Yan 一年内从「别建多 agent」改口「有些 setup 真能用」
- 收束句：责任的叠加上移，不是岗位替代——五种角色，一个人扛

**背景（不上屏，只进 speaker notes）**
- 讲者：问台下"两年前你在干嘛"；金句慢读。
- 角色表 = 说话者→策展者→环境工程师→控制流作者→编排者，对应后面五层（P5–P7），顺序不可乱。
- 转场：这条路是怎么走出来的？五层演变。

---

### P3 · 路线图（1.5 min）

**布局**：顶部标题；四个编号步骤竖排（或横排）；每步一行小注。

**核心上屏**
- 标题：路线图（顶部居中，本页最大字号）
- 副题：今天四站——最后，给你一句能带走的话（标题下，小字）
- 四个步骤：
  1. 五层演变——注意力一层层上移（Prompt → Context → Harness → Loop Engineering → Graph Engineering）
  2. 根本还是 harness
  3. 固定 vs 灵活——你该怎么选
  4. DSH——一个把灵活性交给你的 harness

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 每站一行"带走什么"小注（紧跟各步，小字）：
  - 第 1 站 → 给你一张地图
  - 第 2 站 → 给你一个判断
  - 第 3 站 → 给你一个选择
  - 第 4 站 → 给你一个答案
- 末站强调：最后一句，能带走（预告 slogan）

**背景（不上屏，只进 speaker notes）**
- 第 2、4 站可加粗（对应承重墙与落点）。
- 转场：第一站。

---

### P4 · 发动机（2 min）

**布局**：顶部标题；中部两条证据（数字 + 引文）；中部一条向上箭头链；下部一句强调。

**核心上屏**
- 标题：一条规律，五层演进（顶部居中，本页最大字号）
- 副题：模型越强，工程杠杆越往上走（标题下，小字）
- 证据行1：METR：50% 可靠性任务长度 ≈ 每 7 个月翻倍
- 证据行2："prompt 的精确措辞，越来越不重要" —— Anthropic
- 箭头链：说什么 → 看什么 → 在什么系统里跑（中部，箭头连接）
- 强调：**叠加，不是替代**（底部居中，本页最大字号）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 一句话机制：模型越强 → 单次交互边际收益越低 → 杠杆点越往上移
- 通俗解释：模型每强一分，你敲进 prompt 的那点心思，回报越少
- 旁注：METR 是独立评估机构（非厂商），数字更可信

**背景（不上屏，只进 speaker notes）**
- METR 是独立评估（非厂商）。
- 转场：从第一层开始看。

---

### P5 · Prompt + Context（3 min）

**布局**：顶部标题；中部左右两栏（Prompt | Context）；底部一个引文块。

**核心上屏**
- 标题：从"说什么"到"看什么"（顶部居中，本页最大字号）
- 副题：前两层 Prompt 与 Context：光会说不够，还得看对（标题下，小字）
- 左栏（Prompt）：
  - Prompt 是行为控制面，不是系统本身
  - 把系统问题塞进更长 prompt = 把缺失的控制面伪装成提示复杂度
- 右栏（Context）：
  - Context engineering：决定模型此刻该看到什么
  - context rot：**塞得越多，记得越差**
  - MCP / RAG：能力到位、注意力上移，才成为焦点
- 引文块："The agent has a prompt, a Bash Tool, and an Edit Tool."（最小脚手架）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- Prompt 侧：SWE-bench 最小脚手架——一个 prompt + 一个 Bash Tool + 一个 Edit Tool，就进 SOTA
- Context 侧：Lost in the Middle——长上下文"两头记得好、中间记得差"
- Context 侧：更多 context ≠ 更好回忆
- Context 侧：反年代学——先有 RAG（2023）再有 MCP（2024 末），注意力上移后才成焦点

**背景（不上屏，只进 speaker notes）**
- context rot = token 越多，模型准确回忆越差（两头好中间差）。
- 金句脚手架这一页埋下，P10 会"回收"（雏形早就在）。
- 转场：看对了，但它在什么环境里跑？

---

### P6 · Harness（2.5 min，埋雷）

**布局**：顶部标题；中部一条公式居中大字；公式下六项小标签一行；下部一个容器示意；底部一行警示。

**核心上屏**
- 标题：模型在什么系统里跑（顶部居中，本页最大字号）
- 副题：第三层 Harness：模型之外的一切（标题下，小字）
- 公式：**Agent = Model + Harness** —— Böckeler（居中，本页最大字号）
- 六项标签：权限 / 沙箱 / 工具 / 验证 / trace / CI（公式下横排小字，前缀"模型之外的一切"）
- 容器示意：**单次运行容器**——loop 每次迭代实例化一个 harness；graph 每个节点跑在它自己的 harness 里
- 警示行：⚠️ 2026 安全事件，集中爆发在这一层

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 六项各一句（做成六个小标签卡片）：
  - 权限：什么能做、什么先问
  - 沙箱：文件 + 网络双重隔离
  - 工具：模型能调什么、结果回哪
  - 验证：测试 / lint / 类型
  - trace：记下每一步
  - CI：交付可审计
- 为什么轮到它：prompt / context 不提供可执行环境
- 角色卡：这一层的人 = 环境工程师

**背景（不上屏，只进 speaker notes）**
- 埋雷："记住单次运行容器，第二幕要用"；2026 安全事件点到为止。
- 转场：一次运行稳了，但一次不够。

---

### P7 · Loop Engineering + Graph Engineering（2.5 min）

**布局**：顶部标题；上半段 Loop Engineering（一条循环箭头 + 两行）；下半段 Graph Engineering（两行 + 两点校准）；底部一条结论。

**核心上屏**
- 标题：从"迭代"到"编排"（顶部居中，本页最大字号）
- 副题：第四五层 Loop Engineering 与 Graph Engineering：刚冒头，是方向，不是定论（标题下，小字）
- Loop Engineering 段：
  - Loop Engineering = 反馈驱动迭代：模型越强越能"知错改错"——反馈得当，就不断输出更好结果
  - 黄金法则：只有上次结果改变下次行动，才算 loop
  - "A loop can be wrong in ways that are subtle, expensive, and hard to detect."
- Graph Engineering 段：
  - Graph Engineering = 编排：用 workflow / DAG 把多 agent 的乱发挥收进显式流程（谁先做、何时分支、何时停）
  - 有道理，但只解决特定的事，还在发展——是方向，不是定论
- 结论：**叙事期：火六周就被接棒**（底部，强调）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- Loop Engineering 侧金句："Loop engineering is replacing yourself as the person who prompts the agent." —— Osmani
- Loop Engineering 侧反转：blast radius——loop 错起来更贵（一次错、反复错、放大错）
- Graph Engineering 侧双源定义：state graphs / "From Individual to System Intelligence"
- Graph Engineering 侧：机制不新、诉求新——LangGraph 2024 就有 workflow / DAG
- Graph Engineering 侧：命名先于沉淀——260 万浏览、无框架发布

**背景（不上屏，只进 speaker notes）**
- 口径：260 万，不是 2.6 亿。
- 转场：五层都过完了——五层不是时间线，是一栋楼。

---

### P8 · 叠加观 + 成熟度（2 min）

**布局**：顶部标题；中部五层楼图（纵向堆叠）；底部三行结论。

**核心上屏**
- 标题：五层不是时间线，是一栋楼（顶部居中，本页最大字号）
- 副题：叠加，不是换代——楼越高，地基越关键（标题下，小字）
- 楼图（自下而上）：Prompt → Context → Harness → Loop Engineering → Graph Engineering（harness 层加粗）
- 结论行：
  - 叠加，不是换代：学会用扳手，不等于扔掉螺丝刀
  - 成熟度标尺：前三层在生产里跑；后两层在叙事期
  - 五层里，有一层是其他所有层的**承重墙**（强调）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 成熟度标尺细化：前三层 = 成熟可信；后两层 = 低置信度
- 行动句：今天重仓 1–3 层（把有限的注意力投在能兑现的地方）

**背景（不上屏，只进 speaker notes）**
- 末句抛钩子"下一幕把这面墙找出来"。
- 转场：为什么说 harness 是承重墙？

---

### P9 · 转折（1 min）

**布局**：顶部大字标题；中部两行；底部三个小标签。

**核心上屏**
- 标题：根本还是 harness（顶部居中，本页最大字号）
- 副题：五层讲完，最底下那层是承重墙（标题下，小字）
- 正文：
  - loop 实例化 harness；graph 节点跑在自己的 harness 里
  - 上层是楼层，harness 是地基
- 三个小标签：结构 / 哲学 / 风险（底部横排）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 三线各一句（紧跟小标签）：
  - 结构：loop / graph 都实例化 harness
  - 哲学：反馈同源 + 门禁渗透
  - 风险：2026 CVE（下一页展开）
- 口径脚注：报告将 Context 与 Harness 并列；"harness 最重要"是我们的判断（小字）

**背景（不上屏，只进 speaker notes）**
- 声明：承重墙是我们的判断，不是报告背书。
- 转场：先看定义——harness 到底是什么。

---

### P10 · 定义（1.5 min）

**布局**：顶部标题（公式大字）；中部三行；底部一个引文块。

**核心上屏**
- 标题（公式）：**Agent = Model + Harness**（顶部居中，本页最大字号）
- 副题：harness 就是模型之外的一切（标题下，小字）
- 正文：
  - harness = 模型之外的一切：权限、沙箱、工具、验证、trace、CI
  - 你以为你在用一个聪明的模型，其实你用"模型 + 一整圈系统"
- 引文块：雏形早就在——一个 prompt + 一个 Bash Tool + 一个 Edit Tool

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 大白话：你买很贵的模型买的是"能力"，"可靠性"是周围那一圈给出来的
- 回收句：2024 SWE-bench 最小脚手架，就是最早的 harness

**背景（不上屏，只进 speaker notes）**
- 讲者：回收前文脚手架例子——"那就是最早的 harness"。
- 转场：那 harness 里头到底干什么？两套控制机制。

---

### P11 · 两套控制 + 确定/概率（3 min，全篇最长页）

**布局**：顶部标题；中部一张闭环图（左 Guides 进、右 Sensors 出）；下部两档对比；底部一句原则大字。

**核心上屏**
- 标题：两套控制，一档分野（顶部居中，本页最大字号）
- 副题：Guides 管事前，Sensors 管事后；能确定的，别靠模型猜（标题下，小字）
- 闭环图：
  - **Guides**（前馈）：行动前约束——按这个样式 / 必须过 lint / 只能用这些工具
  - **Sensors**（反馈）：行动后喂回——测试挂了 / 用户拒绝 / 退出码非零
- 两档对比：
  - **computational**（确定性）：测试 / lint / 类型——可复现，可审计
  - **inferential**（概率性）：AI review——意见本身可能错
- 原则：**能交给确定性的，绝不靠概率性的**（底部居中，本页最大字号）
- 附注：Loop Engineering 五级验证梯：同一哲学（小字）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 通俗比喻：Guides = 事前规矩（先洗手、火别开大）；Sensors = 事后纠正（尝一口、咸了加水）
- 通俗对比：npm test 全绿可直接信；AI 说"代码不错"不敢直接信
- 渗透句：同一哲学，往上渗透到 Loop Engineering 五级验证梯

**背景（不上屏，只进 speaker notes）**
- 通俗例：Guides=事前规矩，Sensors=事后纠正；npm test 全绿可直接信，AI 说"代码写得不错"不敢直接信。
- 转场：六件套收成三个动词。

---

### P12 · 圈住 / 拦住 / 看清（2.5 min）

**布局**：顶部标题；中部三张卡片横排；底部一句收束大字。

**核心上屏**
- 标题：三个动词，看懂 harness（顶部居中，本页最大字号）
- 副题：圈住、拦住、看清（标题下，小字）
- 三卡片（左→右）：
  - **圈住**：沙箱 + 权限——文件与网络双重隔离（-84% 权限提示）
  - **拦住**：验证门禁——错误在离源头最近的地方就地拦下
  - **看清**：可观测性 + provenance + CI——trace 记下每一步
- 收束：**模型给能力，harness 给可靠性**（底部居中，本页最大字号）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 六构件归位（小标签）：
  - 圈住 ← 沙箱、权限模型
  - 拦住 ← 工具协议、验证门禁
  - 看清 ← 可观测性、provenance + CI
- 属性句：可靠性是"模型 + harness"系统的属性，不是模型自己的属性

**背景（不上屏，只进 speaker notes）**
- 84% 是 Claude Code 内部实测；"可靠性是'模型+harness'系统的属性"这句慢讲。
- 转场：那为什么说 harness 是承重墙？

---

### P13 · 承重墙（1 min）

**布局**：顶部标题；中部三列（结构 / 哲学 / 风险，各一句引用）；底部一句机制金句。

**核心上屏**
- 标题：承重墙（顶部居中，本页最大字号）
- 副题：连上层自己的定义，都承认 harness 在底下（标题下，小字）
- 三列引用：
  - "Loop sits one floor above the harness." —— Osmani
  - "The harness supplies the engine; loop writes the pilot." —— Macedo
  - GraphARC：plan → check → execute——上层自己在长 harness
- 机制金句：**假设下层正确是 bug 的来源；对下层显式验证，是工程成熟的标志**（底部，引文样式大字）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 三线标注（每列加一个小标签）：
  - 结构线（Osmani / Macedo）
  - 哲学线（反馈同源 + GraphARC 门禁）
  - 风险线（下一页展开）
- 收束句：三层本质同一件事——单次反馈 → 连成循环 → 编排

**背景（不上屏，只进 speaker notes）**
- 三线=结构线 / 哲学线 / 风险线（风险线下一页展开）。
- 口播收束句：三层本质是同一件事——harness 给单次反馈（Sensors），loop 把它连成循环（反馈驱动迭代），graph 把它编排起来（workflow/DAG + 门禁）；归根结底，还是 harness。
- 转场：上面全是正面论证；反面更直接——2026 年真实出过事。

---

### P14 · 2026 CVE（1 min）

**布局**：顶部标题；中部三行（其中一行划线删除）；底部一句修复方向大字。红色调。

**核心上屏**
- 标题：反面证据：2026 年的护栏失效（顶部居中，本页最大字号）
- 副题：不是提示词的问题，是执行时授权缺失（标题下，小字）
- 正文：
  - AWS / Google / Vercel agent 工具触发漏洞
  - ~~不是 prompt injection~~：**执行时授权缺失**
  - 攻击者伪造工具调用，无需运行模型
- 修复方向：**authorize at execution, not at generation**（底部，本页最大字号）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 编号：CVE-2026-18830 / 18236 / 64650 / 64651（小字脚注）
- 机制句：护栏若假设"模型会发出合法工具调用"，工具调用可被伪造时，整条验证链失效
- 意义句：只防 generation、不防 execution，就是不完整

**背景（不上屏，只进 speaker notes）**
- 含义：护栏若假设"模型会发出合法工具调用"，工具调用可被伪造时整条验证链失效。
- 转场：现在回到你——一人公司，这一层你打算怎么办？

---

### P15 · 抉择（1 min）

**布局**：顶部标题；中部两行；下部一条横轴（左固定 → 右灵活）+ 两个小图标。

**核心上屏**
- 标题：固定，还是灵活？（顶部居中，本页最大字号）
- 副题：一人公司的 harness，这一格怎么选（标题下，小字）
- 正文：
  - 不是"自己做 vs 依赖现成"的二选一
  - 真正的区别在一条轴：**固定 vs 灵活**
- 轴示意：固定 ←———→ 灵活（下部横轴）
- 小图标旁注：精装公寓 vs 可改造空间

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 比喻细化：精装公寓好看，但改格局走物业流程；可改造空间，接口标准化
- 立场标注：不是二选一（强调）

**背景（不上屏，只进 speaker notes）**
- 强调"不是二选一"是立场校准。
- 转场：先公平地说，固定 harness 到底好在哪。

---

### P16 · 固定的好（2 min）

**布局**：顶部标题；中部四条清单；底部一条结论。

**核心上屏**
- 标题：现成的价值（顶部居中，本页最大字号）
- 副题：组件随产品交付，开箱即用（标题下，小字）
- 清单：
  - harness 民主化：沙箱 / 权限 / 可观测性，开箱即用
  - Claude Code 沙箱双重隔离；Copilot 云端沙箱
  - "Start with the simplest viable system." —— Anthropic
- 结论：绝大多数人，应从现成开始（底部，强调）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 民主化金句：组件随产品交付——从"自己写 bash 编排"到"开箱即用"
- 明细：provenance 接 CI、沙箱、权限，都替你设计好
- 公平句：常见任务，现成够用

**背景（不上屏，只进 speaker notes）**
- 固定 harness 如 Codex / Claude Code 把沙箱、权限、观测开箱即用。
- 转场：那卡在哪？卡在你想改 harness 本身的时候。

---

### P17 · 固定的卡（3 min）

**布局**：顶部标题；中部四格（每格一个卡点，末尾一个✗）；底部无。

**核心上屏**
- 标题：现成的边界（顶部居中，本页最大字号）
- 副题：当你想改 harness 本身，就卡住了（标题下，小字）
- 四格（左→右、上→下）：
  - 挂自己的模型 / 工具：厂商支持什么，你才能用什么 ✗
  - 换后端：沙箱、工具集定死，没有接口 ✗
  - 挂 skill / knowledge map：只能塞进 prompt（context rot：塞得越多记得越差）✗
  - 补执行时授权？**改不了内核，只能等厂商** ✗（红色）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 本质句：它的 guides / sensors 是写死的、为大众设计的
- 通俗例：精装公寓改格局，走物业流程
- 硬证据：2026 CVE 想补执行时授权，只能等厂商

**背景（不上屏，只进 speaker notes）**
- "等厂商"是情绪高点。
- 转场：所以问题不是"现成不好"，是"只能现成"不行。

---

### P18 · 本质（2.5 min）

**布局**：顶部标题（立场句大字）；中部一个双向箭头公式；下部两列（装自己的 | 借现成的）；底部一行。

**核心上屏**
- 标题：不是别用现成，是别只能现成（顶部居中，本页最大字号）
- 副题：灵活 = 装自己的 + 借现成的（标题下，小字）
- 核心公式：灵活 harness = **装自己的 + 借现成的**（中部，醒目）
- 左列（装自己的）：挂模型 / 挂工具 / 换后端 / 挂 skill / 挂 knowledge map / 自定义门禁
- 右列（借现成的）：成熟 adapter / skill / 模型，直接复用
- 底部一行：不逼你二选一

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 左列明细（小字）：挂模型、挂工具、换后端、挂 skill、挂 knowledge map、自定义可执行门禁
- 右列明细（小字）：成熟的 vendor adapter、skill、模型
- 一人公司句：没有团队分摊基础设施，更需要装得下自己、接得上现成

**背景（不上屏，只进 speaker notes）**
- 对一人公司：没有团队分摊基础设施，更需要装得下自己、接得上现成。
- 转场：有没有这样一个 harness？有——这就是最后要讲的 DSH。

---

### P19 · DSH（1 min）

**布局**：顶部标题；中部一个"内核 + 周围插件"示意图；下部两句 + 一个引文块。

**核心上屏**
- 标题：DSH：把 harness 本身交给你（顶部居中，本页最大字号）
- 副题：没有特权内核，扩展就是挂插件（标题下，小字）
- 引文块："No privileged core to patch: extend by mounting a plugin beside the others."（居中，醒目）
- 正文：
  - 不给你写死的 harness，给你能改它的框架
  - 接口标准化：装自己的，借现成的

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 架构句：无特权内核，扩展 = 挂插件（内核不动，插件围一圈）
- 大白话：能改 harness 本身的框架
- 对比句：精装公寓 vs 能改格局的空间

**背景（不上屏，只进 speaker notes）**
- DSH = DeepSeek Harness；金句含义：没有特权内核，扩展 = 在旁边挂插件。
- 转场：那"灵活"具体长什么样？两句话：装自己的、借现成的。

---

### P20 · 双向灵活（2.5 min）

**布局**：顶部标题；中部一张表（装自己的五行 / 借现成的一行分区）；底部一句金句。

**核心上屏**
- 标题：装自己的，借现成的（顶部居中，本页最大字号）
- 副题：DSH 的实现：挂模型、挂工具、换后端、挂地图（标题下，小字）
- 表（装自己的）：
  - 挂模型：`ctx.llm` 注册 adapter——协议兼容纯配置
  - 挂工具：`ctx.tools` 注册——schema 自动进模型可见面
  - 换后端：capability seam——Definition / Provider / Consumer
  - 挂 skill / knowledge map："Each fact has one home"
- 表（借现成的）：15 行 YAML 挂一个新 vendor
- 金句：加模型、加工具，**都不必改 loop**（底部，强调）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 挂模型补：协议兼容纯配置
- 挂工具补：schema 自动进模型可见面
- 换后端补：Definition / Provider / Consumer，Consumer 不用改
- 挂 skill / knowledge map 补：把自己沉淀的过程性记忆、项目的地图作为一等对象挂进去
- 口径注：DSH 不用 MCP（用 adapter / tools / seam）

**背景（不上屏，只进 speaker notes）**
- DSH 不用 MCP（用 adapter/tools/seam）。
- 转场：最后一句哲学，是 DSH 整个设计的魂。

---

### P21 · 哲学（1.5 min）

**布局**：极简金句页。顶部标题；中部一句英文大字；下部一句中文收束。深色可。

**核心上屏**
- 标题：规则，要由机器执行（顶部居中，本页最大字号）
- 副题：不靠自律，靠系统（标题下，小字）
- 英文金句："Agents follow enforced gates far more reliably than prose conventions."（居中，本页最大字号）
- 中文收束：**不靠自律，靠系统**（居中，次大字）
- 附注：GraphARC 同套：确定性 checker 放行或拒绝（小字）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 行业先例：GraphARC——模型提议，确定性 checker 放行或拒绝
- 回扣句：这就是"确定性优先"的真实样子——自己定义门禁，机器执行
- 一人公司句：项目规则变成机器关卡，不靠自律靠系统

**背景（不上屏，只进 speaker notes）**
- 金句含义：agent 会可靠遵守被强制执行的关卡，而不是 prose 里的约定。
- 转场：所以，回到最开始那个问题。

---

### P22 · 回答 + 带走一句（2 min）

**布局**：顶部标题；中部角色表五格→汇聚；下部 slogan 独占大字（或全屏收束）。深色可。

**核心上屏**
- 标题：一人公司，答案在 harness（顶部居中，本页最大字号）
- 副题：不用从零造，但要能改（标题下，小字）
- 正文：
  - 五种角色，一人承担（责任叠加上移，不是岗位替代）
  - 不用从零造，但要能改
  - 灵活 harness，自动罩住 loop / graph
- slogan：**装自己的，借现成的**（底部居中，全场最大字号）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 角色表回扣五格：说话者 → 策展者 → 环境工程师 → 控制流作者 → 编排者
- 怎么选句：从现成开始，但别停在"只能现成"
- 为什么句：harness 是承重墙；灵活的 harness，自动罩住 loop / graph

**背景（不上屏，只进 speaker notes）**
- 角色表=说话者→策展者→环境工程师→控制流作者→编排者；slogan 慢读、停顿。
- 转场：谢谢大家。

---

### P23 · 收尾（1 min）

**布局**：整页居中；一句"谢谢"大字；slogan 再次放大作落款；底部可留联系方式占位。

**核心上屏**
- 标题：谢谢（居中，本页最大字号）
- 副题：装自己的，借现成的（标题下，次大字）
- 底部一行：[联系方式 / 二维码占位]（小字，用户自填）

**候选上屏素材**（以下内容可以考虑进入 PPT 页面，按版面选用）
- 收拢句：一人公司，答案在 harness（小字，落款上方）

**背景（不上屏，只进 speaker notes）**
- 讲者：一句话收拢——一人公司，答案在 harness；联系方式由用户自行填写。
